# PROGRESS – Projektstand

> Diese Datei wird am Ende jeder Phase aktualisiert. Neue Session? Zuerst hier lesen, dann [CLAUDE.md](CLAUDE.md) für den Gesamtauftrag.

## Status: PHASENPLAN ABGESCHLOSSEN – Phase 15 (Feinschliff) mit Schritt 5 fertig (11.07.2026)

Alle 16 Phasen (0–15) aus CLAUDE.md Abschnitt 7 sind umgesetzt und verifiziert.
Was bewusst offen bleibt (Deployment-Härtung, Lasttests, Multi-Device, DSGVO,
Branding …), steht priorisiert in [ROADMAP.md](ROADMAP.md) – dort zuerst lesen,
wenn es weitergeht.

## Erledigt

### Phase 0 – Setup (05.07.2026)

- Monorepo mit npm-Workspaces: `apps/api` (NestJS 11), `apps/web` (React 19 + Vite + Tailwind 4), `packages/shared` (gemeinsame Typen, CJS-Build)
- Tooling: TypeScript strict (gemeinsame `tsconfig.base.json`), ESLint 9 (Flat Config) + Prettier auf Root-Ebene
- `infra/docker-compose.yml` mit PostgreSQL 16, Redis 7, MinIO (inkl. Healthchecks, Volumes, `.env`-Anbindung)
- API: `GET /api/health` liefert `HealthStatus` (Typ aus `@parley/shared`)
- Web: Statusseite, die `/api/health` über den Vite-Proxy abruft (kein CORS nötig)
- Git-Repo initialisiert (Branch `main`), Commit `dca9dfa`
- Node.js 22 portabel installiert nach `%LOCALAPPDATA%\Programs\nodejs` und in den Benutzer-PATH eingetragen (kein Admin nötig)
- **Nachverifiziert in Session 2:** Docker Desktop wurde installiert, `docker compose up` läuft – alle drei Container healthy

### Phase 1 – Auth & Nutzerverwaltung (05.07.2026)

- **Prisma** angebunden (Schema in `apps/api/prisma/schema.prisma`, Migration `init`): Modelle `User` und `RefreshToken`; Root-Skripte `prisma:generate` / `prisma:migrate`
- **Registrierung & Login** (`POST /api/auth/register|login`): Argon2id-Hashing (OWASP-Parameter: 19 MiB, t=2, p=1), generische Fehlermeldung gegen User-Enumeration
- **Token-Konzept:** Access-Token = JWT (15 min, nur im Speicher des Clients); Refresh-Token = opakes 256-Bit-Token im httpOnly-Cookie (Pfad `/api/auth`, 30 Tage), in der DB nur als SHA-256-Hash
- **Rotation + Diebstahl-Erkennung:** Jeder Refresh entwertet das alte Token; Wiederverwendung nach 60-s-Karenzzeit beendet alle Sessions des Nutzers. Karenzzeit nötig wegen paralleler Refreshes (mehrere Tabs / React StrictMode) – ohne sie loggt sich die App bei jedem Reload selbst aus (in dieser Phase als echter Bug gefunden und behoben)
- **Profil:** `GET/PATCH /api/users/me` (AuthGuard, Bearer-JWT), Status-Text + Avatar-URL änderbar
- **Frontend:** Zustand-Store mit Single-Flight-Refresh und automatischem Retry bei 401, Login-/Registrierungs-Seite, Profilseite mit Status-Bearbeitung und Logout
- **Vitest** eingerichtet (`apps/api`, Root-Skript `npm test`), Unit-Tests für Token-Utilities
- `.env` im Root angelegt (JWT_SECRET generiert), `.env.example` erweitert

**Verifiziert (Ende-zu-Ende über die UI):** Registrieren → Profilseite; Status speichern → übersteht Reload; Session-Restore nach Reload; Logout → zurück zum Login; falsches Passwort → generische Fehlermeldung; korrektes Login → Profil. In der DB: nur `$argon2id$…`-Hashes, Refresh-Tokens nur als Hash, nach Logout alle widerrufen. Build/Tests/Lint/Format grün.

### Phase 2 – Echtzeit-Gateway (05.07.2026)

- **Eigenes Opcode-Protokoll** in `packages/shared/src/gateway.ts`: HELLO → IDENTIFY (JWT) → READY → HEARTBEAT/ACK, Dispatch-Events (`READY`, `PRESENCE_UPDATE`), definierte Close-Codes (4001–4004)
- **Gateway-Server** (`apps/api/src/gateway/`): natives `ws` am selben HTTP-Server (Pfad `/gateway`), Identify-Timeout 10 s, Heartbeat-Intervall 15 s mit 2×-Toleranz, Payload-Limit 64 KiB
- **Presence in Redis** (`presence:conn:{userId}`-Zähler mit 60-s-TTL als Totmann-Schalter + `presence:users`-Hash); mehrere Tabs = ein Nutzer online; Leichen werden beim Snapshot lazy aufgeräumt
- **Multi-Instanz-fähig:** alle Events laufen über Redis-Pub/Sub (`gateway:dispatch`), auch für die eigene Instanz – ein Codepfad für 1..N Gateways
- **Rate-Limiting** (aus ROADMAP nachgezogen): `RateLimitGuard` (Redis, Fixed Window, pro IP) auf register (10/5 min), login (10/min), refresh (60/min)
- **Frontend:** `GatewayClient` (Reconnect mit exponentiellem Backoff, Heartbeat-Überwachung, Token-Refresh bei Close 4001), Presence-Store (Zustand), Online-Panel auf der Profilseite, `/gateway`-WS-Proxy in Vite
- **Vite-Alias** `@parley/shared` → TS-Quelle (Rollup kann CJS-Enum-Re-Exports nicht auflösen)

**Verifiziert:** Protokoll-Test-Skript (2 Nutzer: READY-Snapshot, gegenseitige PRESENCE_UPDATEs online/offline, HEARTBEAT_ACK, Close 4001 bei ungültigem Token) – alle Checks grün. UI-Test im Browser: „Gerade online“-Panel zeigt zweiten Nutzer in Echtzeit beim Verbinden/Trennen. Rate-Limit: 12 Login-Versuche → 2× HTTP 429. Build/Tests/Lint grün.

### Phase 3 – Server & Kanäle (05.07.2026)

- **Prisma:** Modelle `Server`, `Membership` (Composite-Key userId+serverId), `Channel` (+ `ChannelType`-Enum, serverId nullable für DMs ab Phase 7); Migration `servers_channels`
- **REST** (`apps/api/src/servers/`): Server-CRUD, Beitritt per Server-ID (Invites folgen Phase 12), verlassen (Owner gesperrt), Kanal-CRUD; Verwaltung bis Phase 5 Owner-only; 404 statt 403 für Nicht-Mitglieder (kein Existenz-Leak), 409 bei Doppel-Beitritt
- **Gateway erweitert:** `publishDispatch(t, d, targetUserIds?)` – Events gehen gezielt an Server-Mitglieder (`SERVER_UPDATE/DELETE`, `SERVER_MEMBER_ADD/REMOVE`, `CHANNEL_CREATE/UPDATE/DELETE`)
- **Frontend:** Discord-artiges Layout (Server-Leiste → Kanal-Sidebar → Hauptbereich → Mitglieder-Panel), Server/Kanal-Dialoge, „ID kopieren“-Button zum Einladen, Mitglieder-Panel mit Live-Online-Status, Profil als Unteransicht (⚙ → Zurück); `GatewayClient` mit generischem `onDispatch`, Store-Verdrahtung in `lib/gatewayConnection.ts`; nach Reconnect wird der REST-Stand neu geladen (verpasste Events)

**Verifiziert:** UI: Server „Arians Treffpunkt“ + Kanal „projekte“ angelegt; frieda tritt per ID bei und erscheint OHNE Reload im Mitglieder-Panel (SERVER_MEMBER_ADD). API: Details vor Beitritt 404, Beitritt 201, Doppel-Beitritt 409, Kanal anlegen als Nicht-Owner 403, Server löschen als Nicht-Owner 403 – serverseitig durchgesetzt. Build/Tests/Lint grün.

### Phase 4 – Basis-Text-Chat, unverschlüsselt (05.07.2026)

- **Prisma:** `Message` (content als Klartext – wird in Phase 6 durch ciphertext+nonce ersetzt), Index `(channelId, createdAt)`; Migration `messages`
- **REST** (`apps/api/src/messages/`): `POST/GET /api/channels/:id/messages`; History rückwärts paginiert (50er-Seiten, `before`-Cursor, `hasMore` über N+1-Trick); Zugriff nur für Server-Mitglieder in TEXT-Kanälen (404 statt 403); Senden rate-limitiert (30/30 s)
- **Gateway:** `MESSAGE_CREATE` an alle Server-Mitglieder
- **Frontend:** `ChatView` (Verlauf mit „Ältere laden“, Gruppierung aufeinanderfolgender Nachrichten, Auto-Scroll nur wenn unten, Fehleranzeige mit Draft-Erhalt), Messages-Store mit Dedupe (REST-Antwort vs. Gateway-Event)

**Verifiziert:** arian (UI) ↔ frieda (Skript am Gateway): Nachrichten kommen in beide Richtungen live an; History übersteht Reload; Nicht-Mitglied bekommt 404 auf Lesen UND Senden (serverseitig). Build/Lint grün.

### Phase 5 – Rollen & Berechtigungen (06.07.2026)

Die Implementierung kam als Commit `39b9ec7` („initial commit rijon“) von Rijon; diese Session hat den Code reviewt, Lücken geschlossen und die Phase verifiziert.

- **Bitfield** in `packages/shared/src/permissions.ts`: BigInt (über JSON als Dezimal-String), 9 Rechte (ViewChannels, SendMessages, ManageChannels, ManageRoles, ManageServer, Kick/Ban, ManageMessages, Administrator); unbekannte Bits werden beim Schreiben maskiert
- **Standardrolle „Mitglied“** (isDefault, View|Send) pro Server, gilt implizit für alle Mitglieder – nicht löschbar/zuweisbar, Name fix; Backfill-Migration versorgt Bestands-Server
- **Effektive Rechte:** Owner → Administrator; sonst Standardrolle ∪ zugewiesene Rollen (Bit-OR). Zentral im `PermissionsService`: 404 für Nicht-Mitglieder (kein Existenz-Leak), 403 bei fehlendem Recht – in jedem Endpunkt serverseitig
- **REST:** Rollen-CRUD + Zuweisung (`PUT/DELETE /servers/:id/members/:userId/roles/:roleId`) unter ManageRoles; bestehende Endpunkte umgestellt (Server-PATCH → ManageServer, Kanal-CRUD → ManageChannels, Senden → SendMessages, History → ViewChannels); Server-DELETE bleibt Owner-only
- **Gateway:** `ROLE_CREATE/UPDATE/DELETE`, `MEMBER_ROLES_UPDATE`. **Review-Fix:** `MESSAGE_CREATE` geht nur noch an Mitglieder mit ViewChannels (`getMemberIdsWithPermission`) – vorher hätte das Gateway live zugestellt, obwohl die REST-History gesperrt war
- **Frontend:** `RolesDialog` (anlegen, Rechte togglen, zuweisen, löschen), Rollen-Badges im Mitglieder-Panel, `myPermissions` in `ServerDetails`; UI blendet nur aus (Kanal-+, Rollen-Button, Eingabefeld). Review-Fix: `PUT` fehlte im Methoden-Typ von `authFetch`
- **Nebenbei repariert:** `node_modules` unvollständig + Prisma-Client veraltet (npm install, prisma generate), 3 nicht eingespielte Migrationen deployed; `.gitattributes` erzwingt jetzt LF (der frische Checkout mit `core.autocrlf=true` hatte alle Dateien auf CRLF gestellt → Prettier schlug überall fehl), Working Tree auf LF normalisiert

**Verifiziert:** Skript mit 33 Checks, alle grün – Kerntest: Senden ohne Schreibrecht → **403 vom Server**; 404 statt 403 für Nicht-Mitglieder; alle Verwaltungs-Endpunkte ohne Recht 403; Rollenzuweisung schaltet Rechte frei, Entzug sperrt wieder; Gateway liefert `MESSAGE_CREATE` nur mit ViewChannels (mit und ohne getestet); Standardrolle geschützt (Löschen/Zuweisen → 400, Name unveränderbar); unbekannte Bits maskiert. UI: Rollen-Dialog geprüft (Rolle angelegt, Recht getoggelt – Zustand kommt über ROLE_UPDATE-Event zurück). Build/Tests/Lint/Format grün.

### Phase 6 – Ende-zu-Ende-Verschlüsselung (06.07.2026)

Wie Phase 5 kam die Implementierung als angelieferter Commit (`3a38c88`); diese Session hat den Krypto-Code reviewt, vier Lücken geschlossen und die Phase verifiziert.

- **Krypto-Bausteine** in `packages/shared/src/crypto/` (nur libsodium-Primitiven, 10 Unit-Tests):
  - Identität = Ed25519 (signieren) + Konvertierung nach X25519 (DH), signierter Prekey; Server prüft die Prekey-Signatur beim Upload
  - **X3DH** nach Signal-Spec (Fallback-Modus ohne One-Time-Prekeys, siehe ROADMAP), KDF = keyed BLAKE2b
  - **Double Ratchet** (BLAKE2b-Ketten, XChaCha20-Poly1305, Header als AD, MAX_SKIP-Schutz); alle Funktionen pure – Zustand wird erst nach erfolgreichem Ver-/Entschlüsseln persistiert
  - **Sender-Key-Ratchet** für Kanäle (Megolm-Prinzip): Kette + Ed25519-Signatur (Mit-Mitglieder können nichts fälschen), Entschlüsseln idempotent (frühester Stand + Vorspulen), AD bindet die Kanal-ID
- **API:** `Device` (ein Gerät pro Account, nur öffentliche Schlüssel) + `KeyEnvelope`-Mailbox (Ende-zu-Ende-verschlüsselte Sender-Key-Verteilung, Ack = DELETE, Live-Zustellung als `KEY_ENVELOPE`-Event); `PUT /keys`, `GET /users/:id/keys`, `POST/GET/DELETE /envelopes`; `Message` speichert nur noch `ciphertext`+`nonce`+`header` (Migration `e2ee` löscht die Klartext-Bestände); Identitätswechsel = Schlüssel-Reset (verwirft liegengebliebene Umschläge)
- **Web-Client:** `lib/e2ee.ts` (Schlüssel in IndexedDB pro Nutzer, Web-Lock für Multi-Tab, Sender-Key-Verteilung vor dem Senden, Rotation nach `SERVER_MEMBER_REMOVE`), entschlüsselte Texte nur im Speicher, 🔒-Platzhalter für (noch) nicht entschlüsselbare Nachrichten, 🔒-Badge im Kanal-Header
- **Review-Fixes dieser Session:** (1) Schlüssel-Reset eines Mitglieds wird bei der Sender-Key-Verteilung jetzt erkannt (`distributedTo` vergleicht den Identitätsschlüssel – vorher wäre das Mitglied dauerhaft ohne Schlüssel geblieben); (2) bereits verarbeitete Umschläge werden erneut geackt, falls das Lösch-DELETE damals fehlschlug; (3) fehlgeschlagene E2EE-Initialisierung friert nicht mehr bis zum Reload ein (Reconnect versucht es erneut); (4) unbehandelte Promise-Rejections im Gateway-Dispatcher beseitigt

**Verifiziert:** Skript mit 33 Checks, alle grün – u. a.: DB enthält nur Ciphertext (`content`-Spalte weg, kein Klartext, keine privaten Schlüssel, Chain-Keys in Umschlägen nur verschlüsselt); X3DH+Ratchet-Umschlag offline (Mailbox) und live (Gateway); Vorspulen/Out-of-Order; **Beitritts-Semantik** (Nachrichten vor dem Beitritt bleiben unlesbar); **Rotation nach Austritt** (Ausgetretener: kein Event, History 404, alte Schlüssel passen nicht); Schlüssel-Reset verwirft Mailbox; Validierung (ungültige Prekey-Signatur 400, Umschlag >16 KiB 400, ohne Header 400). Interop-Test Web-Client ↔ Skript-Client: UI-Nachricht wird nach friedas Schlüssel-Reset automatisch neu verteilt und entschlüsselt, friedas verschlüsselte Antwort erscheint lesbar in der UI, übersteht Reload (IndexedDB); alte, fremdverschlüsselte Nachrichten zeigen den 🔒-Platzhalter. Build/Tests (21)/Lint/Format grün.

### Phase 7 – Direktnachrichten & Freunde (07.07.2026)

Wie Phase 5/6 kam die Implementierung als angelieferter Commit (`8cd249e`); diese Session hat reviewt, drei Lücken geschlossen und die Phase verifiziert.

- **Prisma:** `Friendship` (eine Zeile pro Richtung, `userId` = Initiator des aktuellen Zustands: PENDING/ACCEPTED/BLOCKED; gegenseitige Blocks möglich), `DmMember` (n:m, v1 = genau zwei), `Channel.dmKey` (kanonischer Paar-Schlüssel „kleinereId:größereId“ mit Unique-Index – verhindert doppelte DM-Kanäle beim gleichzeitigen Öffnen, Race wird per P2002-Catch aufgelöst); Migration `friends_dms`
- **Freunde-REST** (`apps/api/src/friends/`): Anfrage per Benutzername (Gegen-Anfrage wird direkt angenommen), annehmen, ablehnen/zurückziehen/entfreunden (ein DELETE), blockieren/entblocken. Kein Leak: Wer mich blockiert, taucht bei mir nirgends auf; Anfrage bei Blockierung in irgendeiner Richtung → generisches 400. Jede Änderung löst `FRIENDS_UPDATE` an beide aus (Clients laden die Liste neu)
- **DM-REST** (`apps/api/src/dms/`): `GET/POST /api/dms`; Policy: DMs zwischen Freunden ODER Mitgliedern gemeinsamer Server, Blockierung sperrt Öffnen und SENDEN in beide Richtungen (History bleibt lesbar, wie Discord). `MessagesService` behandelt DM-Kanäle (nur die zwei Teilnehmer, 404 für Außenstehende) neben Server-Kanälen
- **DM-E2EE:** DMs nutzen denselben Sender-Key-Ratchet wie Kanäle (verteilt über die X3DH+Double-Ratchet-Sessions aus Phase 6) – **bewusste Abweichung** von CLAUDE.md §6 (reines Double Ratchet), weil History ohne lokalen Klartext-Speicher nach Reload neu entschlüsselbar sein muss; Trade-off in ROADMAP.md dokumentiert
- **Presence gescoped:** READY-Snapshot und `PRESENCE_UPDATE` gehen nur noch an den Sichtbarkeitskreis (`VisibilityService`: Freunde + gemeinsame Server + DM-Partner); `PRESENCE_SYNC` liefert additiv nach, wenn der Kreis wächst (Server-Beitritt); neue Freundschaft pusht den Online-Status an beide
- **Sichtbarkeits-Härtung:** `POST /api/envelopes` (403) und – Review-Fix dieser Session – `GET /users/:id/keys` (404, kein Existenz-Leak per UUID-Raten) verlangen den Sichtbarkeitskreis
- **Frontend:** Home-Ansicht (Zuhause-Button mit Anfragen-Badge → DM-Liste mit Online-Punkten + Freunde-Panel mit Tabs Freunde/Anfragen/Blockiert), `ChatView` im DM-Modus (@ statt #, kein Rechte-Gate), Stores `friends`/`dms`, neue Gateway-Events verdrahtet
- **Review-Fixes dieser Session:** (1) `accept()` räumt die Gegen-Anfrage mit ab, falls sich beide gleichzeitig angefragt haben (sonst bliebe eine PENDING-Zeile für immer liegen); (2) Schlüsselbündel-Abruf auf Sichtbarkeitskreis beschränkt (s. o.); (3) Lint-Fehler (ungenutztes `get` im DMs-Store)

**Verifiziert:** Skript mit 41 Checks, alle grün – u. a.: kompletter Anfrage-Lebenszyklus inkl. Gegen-Anfrage-Auto-Accept; Presence-Scoping (Fremde sehen sich weder im READY-Snapshot noch per PRESENCE_UPDATE; neue Freundschaft/Server-Beitritt liefern nach); DM-Policy (Freunde ✓, gemeinsamer Server ✓, Fremde 403, selbst 400, kanonische Kanal-ID von beiden Seiten); E2EE-Roundtrip in beide Richtungen; Blockieren (Senden 403 beidseitig, History lesbar, DM-Öffnen 403, generische 400er, kein Leak zur Gegenseite, Entblocken stellt Senden wieder her); Bundle/Umschlag-Sichtbarkeit. DB-Check: nur Ciphertext, DM-Kanal ohne Server mit dmKey. Interop-Test UI ↔ Skript: Anfrage-Badge live, Annehmen in der UI, verschlüsselte DMs in beide Richtungen lesbar, übersteht Reload (IndexedDB), Konsole sauber. Build/Tests (21)/Lint/Format grün.

### Phase 8 – Datei-/Bild-Uploads (07.07.2026)

Wie Phase 5–7 kam die Implementierung als angelieferter Commit (`c94f9b3`); diese Session hat reviewt, vier Lücken geschlossen und die Phase verifiziert.

- **Prisma:** `Attachment` (channelId, uploaderId, messageId nullable bis zum Senden, objectKey, sizeBytes); Migration `attachments`. **Bewusste Abweichung von CLAUDE.md §5:** kein `mimeType`/`encryptedFileUrl` in der DB – Dateiname, MIME-Typ, Klartextgröße und Dateischlüssel stehen IM E2EE-Nachrichtentext (`MessageContentV1` in `@parley/shared`), der Server sieht nur Blob + Größe
- **Krypto:** `packages/shared/src/crypto/file.ts` – XChaCha20-Poly1305 mit frischem Zufallsschlüssel pro Datei (Prinzip wie Signal/Matrix-Anhänge); 5 neue Unit-Tests (Datei-Roundtrip, Inhaltsformat inkl. Phase-6/7-Rohtext-Fallback)
- **API:** `StorageService` (MinIO, Bucket-Anlage beim Start) + `AttachmentsModule`: `POST /channels/:id/attachments` (roher `application/octet-stream`-Body statt Multipart, ≤ 10 MiB + AEAD-Overhead, Rechte wie Senden inkl. DM-Blockierung), `GET /attachments/:id` (Stream, nur Kanal-Mitglieder, ungebunden nur Uploader, 404 für Fremde). Senden bindet `attachmentIds` **transaktional** (nur eigene, unverbrauchte Uploads desselben Kanals, max. 10/Nachricht – sonst Rollback inkl. Nachricht). Zugriffslogik aus `MessagesService` in gemeinsamen `ChannelAccessService` extrahiert
- **Web-Client:** `lib/attachments.ts` (verschlüsseln → hochladen → Metadaten in den E2EE-Klartext; Bilder bekommen ein Canvas-Thumbnail ≤ 384 px als eigenen verschlüsselten Blob; Download-Cache als Object-URLs, Reset bei Logout), `AttachmentView` (Bild-Vorschau, Klick = Original speichern; Datei-Chip mit Speichern-Button), Datei-Auswahl mit Chips in der Eingabezeile (max. 4 Dateien/Nachricht)
- **Review-Fixes dieser Session:** (1) Kanal-/Server-Löschung entfernt jetzt auch die MinIO-Blobs (`removeAllWithPrefix`, best-effort nach dem DB-Delete – vorher blieben sie für immer liegen); (2) Aufräum-Job für nie gebundene Uploads (beim Start + stündlich, Frist 6 h; vorher unbegrenzter Gratis-Speicher per Upload-ohne-Senden); (3) Ciphertext-Limit der Nachrichten 24 KiB → 32 KiB (Worst Case 4000 Nicht-ASCII-Zeichen + 10 Anhangs-Metadaten passte nicht); (4) Thumbnail-Anzeigegröße in der UI geklemmt (Maße sind absenderkontrollierte E2EE-Metadaten – manipulierte Werte hätten das Layout gesprengt). Nebenbei: `'upload'`-Intent im `ChannelAccessService` spart die Empfängerliste, die nur das Senden braucht

**Verifiziert:** Skript mit 38 Checks, alle grün – u. a.: Upload-Validierung (leer/zu groß/413/kein octet-stream/Nicht-Mitglied 404); Binde-Regeln (fremd/doppelt/kanalfremd/>10 → 400, Transaktion hinterlässt keine Nachricht); Download-Rechte (Mitglied ✓, Fremder 404, ungebunden nur Uploader); voller E2EE-Roundtrip Skript↔Skript (Blob byte-identisch, Klartext-Marker NICHT im Ciphertext, Datei entschlüsselt identisch); DM-Uploads inkl. Blockierung (403) und Entblocken; DB nur technische Spalten; Blob physisch in MinIO; Kanal-Löschung räumt Blobs ab; Start-Cleanup entsorgt gealterte ungebundene Uploads (DB + MinIO). Interop UI↔Skript: Bild aus der UI (arian) → frieda (Skript) entschlüsselt Text, PNG-Original und JPEG-Thumbnail (Magic Bytes geprüft); friedas verschlüsselte Antwort mit Datei-Chip erscheint live und lesbar in der UI, übersteht Reload (Thumbnail wird neu geladen und entschlüsselt), Konsole sauber. Build/Tests (26)/Lint/Format grün.

### Phase 9 – Reaktionen, Threads, Erwähnungen, Suche (07.07.2026)

Wie Phase 5–8 kam die Implementierung als angelieferter Commit (`34d0b6a`); diese Session hat reviewt, fünf Lücken geschlossen und die Phase verifiziert. Kernprinzip: **Alles reist IM Ciphertext** – kein Backend-Change nötig, der Server kann Reaktionen nicht von Textnachrichten unterscheiden und sieht weder Emojis noch Antwort-Graphen noch Erwähnungen.

- **Shared (`MessageContentV1` erweitert):** `replyTo` (Referenz + eingebettete Zitat-Vorschau ≤ 160 Zeichen, Prinzip wie Signal) und `reaction` (targetMessageId, Emoji, add/remove) im E2EE-Klartext; `decodeMessageContent` validiert absenderkontrollierte Felder defensiv (kaputte Felder → null, Rohtext aus Phase 6/7 bleibt lesbar)
- **Reaktionen:** verschlüsselte Spezial-Nachrichten in derselben Pipeline, Toggle-Semantik; der Store faltet pro (Ziel, Nutzer, Emoji) das jüngste Event ein, `MessageRow` aggregiert (Zähler, „von mir“, Schnellauswahl-Picker mit 8 Emojis); Reaktions-Events erscheinen nicht im Verlauf
- **Antworten/Threads:** Antwort-Banner in der Eingabezeile, Zitat-Zeile über der Nachricht („Zum Original springen“ mit Aufleuchten), Thread-Ansicht = Wurzel + alle Nachfahren, clientseitig aus den replyTo-Bezügen abgeleitet
- **Erwähnungen:** clientseitige Erkennung (`@name` gegen bekannte Kanal-Benutzernamen, case-insensitiv), Highlight (gelb) für eigene Erwähnungen, Browser-Notification per Notification-API (🔔-Button, nur bei unfokussiertem Tab; echte Push-Benachrichtigungen folgen in Phase 12)
- **Suche (🔍):** nur im geladenen, entschlüsselten Verlauf (Text + Anhangs-Namen), mit Treffer-Zähler – serverseitig wegen E2EE prinzipbedingt unmöglich
- **Review-Fixes dieser Session:** (1) Thread-Wurzelsuche gegen Antwort-Zyklen abgesichert (mit UUID-IDs nicht fälschbar, aber die Terminierung soll nicht von dieser Invariante abhängen); (2) `CSS.escape` + ID-Format-Validierung (`[A-Za-z0-9-]{1,64}`) für absenderkontrollierte Nachrichten-Referenzen – ohne sie hätte ein `"]` in replyTo.messageId den `querySelector` im Click-Handler werfen lassen; (3) Emoji-Plausibilitätscheck (`isPlausibleReactionEmoji`: nur Emoji-Bausteine, nicht rein ASCII) – vorher hätte sich beliebiger 32-Zeichen-Text als „Reaktion“ in fremde UIs rendern lassen; (4) Reaktions-/Antwort-Aktionen ohne SendMessages ausgeblendet (Server blockte schon mit 403, die UI bot die Buttons aber an); (5) Reaktions-Falten deterministisch gemacht (Event-ID als Tiebreaker bei gleichem Millisekunden-Zeitstempel – sonst könnten Clients dauerhaft unterschiedliche Stände zeigen)
- **Bewusst NICHT in dieser Phase:** Nachrichten bearbeiten/löschen (→ Phase 13, siehe ROADMAP), Erwähnungs-Picker/ID-Erwähnungen, Nachladen des Originals beim Zitat-Klick (alles in ROADMAP notiert)

**Verifiziert:** Shared-Unit-Tests 21 (neu: Emoji-Plausibilität, ID-Validierung, replyTo-Roundtrip). Skript mit 19 Checks, alle grün – u. a. replyTo-Roundtrip mit Preview-Klemmung, Reaktions-add/remove in beide Richtungen (live über Gateway), fünf böswillige Payloads (Text-als-Emoji, ASCII-Bausteine, Selector-Injektion, kaputte IDs, unbekannte action) werden beim Empfänger verworfen, Reaktions-Events liegen als normale Nachrichten in der History. DB-Check: Header eines Reaktions-Events enthält nur `{iteration,keyId,signature,v}`, kein Klartext-/Emoji-/Erwähnungs-Leak in Ciphertext oder Header. Interop UI↔Skript: UI-Nachricht wird vom Skript entschlüsselt; Skript-Antwort (Zitat-Zeile), ❤️-Reaktion (Chip), Erwähnung (gelbes Highlight) und Geist-Zitat auf nicht geladene Nachricht erscheinen live und korrekt in der UI; UI-Reaktion 👍 add→remove kommt beim Skript entschlüsselt und korrekt an; Thread-Ansicht (2 Nachrichten, sauberes Schließen), Sprung-zum-Original mit Aufleuchten, Suche (1 Treffer); Reload: Reaktionen werden aus der History neu gefaltet, Highlight/Zitat intakt; Konsole sauber. Build/Tests (32)/Lint/Format grün.

### Phase 10 – Sprachchat (08.07.2026)

Erste Phase mit neuem Teilprojekt (`apps/voice`, mediasoup-SFU) und selbst gebaut (kein angelieferter Commit). Zwei getrennte Signaling-Ebenen: **Roster/State** (beitreten/verlassen, Mute/Deafen, „wer ist im Kanal") über REST + bestehendes Gateway; **Medien** (WebRTC-Transport/Produce/Consume) über eine direkte WebSocket zum SFU. Medien sind zunächst nur transportverschlüsselt (DTLS-SRTP); Medien-E2EE steht in der ROADMAP.

- **mediasoup läuft portabel:** `npm install mediasoup@3` lädt ein vorgebautes `mediasoup-worker.exe` (kein Compiler/Python/MSVC nötig) – Worker startet, Router + WebRTC-Transporte funktionieren
- **Shared** (`packages/shared/src/voice.ts`): Voice-Signaling-Protokoll (Client⇄SFU, opake mediasoup-Wire-Objekte als `unknown`, damit shared nicht von mediasoup abhängt), `VoiceState`, `VOICE_STATE_UPDATE`-Event, `VoiceJoinResponse`, `VoiceTokenClaims`; `ServerDetails.voiceStates`; `CreateChannelRequest.type`
- **Prisma:** `VoiceSession` (userId unique = ein Nutzer/ein Sprachkanal, muted/deafened); Migration `voice`. **Abweichung von CLAUDE.md §5:** kein `sfuPeerId` – Peer-Identität lebt im SFU. Beim API-Start werden verwaiste Sessions geleert
- **API** (`apps/api/src/voice/`): `POST /voice/channels/:id/join` (Rechte = ViewChannels, VoiceSession-Upsert, Broadcast, kurzlebiges **Voice-Token** = JWT mit channelId/purpose), `POST …/leave`, `PATCH …/state` (Mute/Deafen, Deafen erzwingt Mute); Roster-Broadcast an Mitglieder mit ViewChannels (wie MESSAGE_CREATE); SFU meldet Trennungen via Redis `voice:disconnect` → Roster-Cleanup (crash-fest). Kanal-Erstellung akzeptiert jetzt `type` (TEXT/VOICE)
- **SFU** (`apps/voice/`, neues Workspace): eigenständiger Node-Dienst (mediasoup, ws, ioredis, jsonwebtoken; `tsx` für Dev). Ein Worker, ein Router pro Kanal (`Room`), Peer pro Medien-WS; Signaling-Protokoll (auth→welcome, getRtpCapabilities, createTransport, connect, produce, consume, resume, pause; Benachrichtigungen newProducer/producerClosed/peerLeft). Zweit-Verbindung desselben Nutzers ersetzt die alte ohne fälschliches Trenn-Signal
- **Web** (`apps/web`): `lib/voice.ts` (mediasoup-client: Device, Send-/Recv-Transporte, Mikro-Producer, Consumer + versteckte `<audio>`-Elemente; **ohne Mikro sauberer Zuhörer-Modus** statt Fehler), `store/voice.ts` (Beitritt/Verlassen/Mute/Deafen + Roster), Kanal-Sidebar mit getrennten Text-/Sprach-Sektionen und Teilnehmerliste je Sprachkanal, `VoicePanel` (verbunden/Mute/Deafen/Trennen, bleibt server-übergreifend sichtbar), Kanal-Erstellung mit Typ; nach Gateway-Reconnect wird die Voice-Session serverseitig neu registriert; Vite-Proxy `/voice`
- **Infra:** `.env`/`.env.example` (VOICE_PORT, VOICE_PUBLIC_URL, MEDIASOUP_*), `scripts/dev-voice.cmd`, launch.json-Eintrag `voice`, Root-Skripte `dev:voice`/`start:voice`, Voice im Root-`build`

**Verifiziert:** Signaling-Skript mit **36 Checks, alle grün** (zwei Nutzer gegen den echten SFU, fabrizierte gültige Opus-RTP-Parameter): Beitritts-Autorisierung (Nicht-Mitglied/Text-Kanal/unbekannt → 404), Join → Token + Roster-Broadcast + `voiceStates` in ServerDetails, Medien-Handshake (welcome, Opus-Caps, Transport mit ICE/DTLS, produce), cross-peer getProducers/consume/resumeConsumer/newProducer, Mute (+pauseProducer) und Deafen (Server erzwingt Mute), ungültiges Token → authError, explizit Verlassen (Broadcast null + SFU peerLeft + Roster-Cleanup), Absturz-Cleanup (WS-Close → Redis → Roster), Kanalwechsel (eine Session). **Browser-Test** (Preview): Sprachkanal anlegen (Typ-Umschaltung), Beitritt → Zuhörer-Modus („Kein Mic", getUserMedia headless → sauberer Fallback), Self im Roster, VoicePanel „Sprache verbunden"; zweiter (Node-)Teilnehmer erscheint live im Roster und wird **real konsumiert** (Audio-Element mit lebendem Track = echter ICE/DTLS-Handshake zum SFU), Deafen schaltet den eingehenden Strom stumm (`audio.muted`), Verlassen baut Medien ab und entfernt Self. Build/Tests (32)/Lint/Format grün.
_Hinweis:_ Echtes 2-Wege-Audio zwischen zwei Menschen braucht Mikrofone (manueller Test) – die Medien-Kontrollebene ist automatisiert vollständig verifiziert.

### Phase 11 – Video & Bildschirmfreigabe (08.07.2026)

Selbst gebaut auf der Phase-10-SFU-Infrastruktur. Kern: Video (Kamera) und Bildschirmfreigabe über dieselbe mediasoup-Ebene wie Audio; eine `source`-Kennung (`mic`/`cam`/`screen`) unterscheidet die Producer, damit Clients Kamera von Screen-Share trennen können.

- **Shared** (`packages/shared/src/voice.ts`): `VoiceSource = 'mic'|'cam'|'screen'`; `produce`-Opcode trägt jetzt `source`; neuer Opcode `closeProducer` (Kamera aus / Freigabe beenden – anders als `pauseProducer` verschwindet der Stream bei den anderen ganz); `VoiceProducerInfo.source`; `VoiceState`/`VoiceStateUpdatePayload`/`VoiceStateChangeRequest` um `cameraOn`/`screenOn` erweitert
- **SFU** (`apps/voice`): Router-Codecs um **VP8 und H264** erweitert (Chrome/Firefox + Safari); `produce` legt `source` als `appData` am Producer ab, `producerInfo` reicht sie über `newProducer`/`getProducers` weiter; `closeProducer` schließt den Producer serverseitig und meldet `producerClosed`
- **Prisma:** `VoiceSession.cameraOn`/`screenOn` (Migration `voice_video`); beim (Neu-)Beitritt zurückgesetzt. API (`voice.service`) persistiert/broadcastet sie, `updateState`-DTO validiert alle vier Booleans, Server-Details-Snapshot liefert sie an Spätbeitreter
- **Web-Client** (`lib/voice.ts`): Kamera-/Screen-Producer (`startCamera`/`stopCamera`/`startScreenShare`/`stopScreenShare`), `getDisplayMedia`, `source` als appData durch den `produce`-Handler; fremde Video-Producer werden konsumiert und als **Video-Kacheln** (`VideoTile`) an den Store gemeldet (Audio bleibt verstecktes `<audio>`); Producer-Metadaten-Map ordnet Consumer die Quelle/Nutzer zu; nativer „Freigabe beenden"-Button (`track 'ended'`) räumt sauber auf und zieht das Roster nach
- **Store** (`store/voice.ts`): `selfCameraOn`/`selfScreenOn`/`videoTiles`, `toggleCamera`/`toggleScreenShare`, `syncState` pusht alle vier Booleans (Reconnect-Rejoin zieht sie nach)
- **UI:** neue `VoiceStage` (Video-Raster über dem Chat, spiegelt die eigene Kamera, beschriftet Kachel mit Nutzer + Kamera/Bildschirm), Kamera-/Teilen-Buttons im `VoicePanel`, 📹/🖥️-Indikatoren im Sprachkanal-Roster der Sidebar

**Verifiziert:** Signaling-/Roster-Skript mit **27 Checks, alle grün** (zwei Peers gegen den echten SFU + echte API/DB): Roster-Round-trip von `cameraOn`/`screenOn` (Broadcast an Gateway, DB-Snapshot für Spätbeitreter, Unabhängigkeit von Mute), DTO-Validierung (fehlendes Feld → 400); Router führt Opus **+ VP8 + H264**; `closeProducer(unbekannt)` → Fehler; **fabrizierter VP8-Kamera-Producer** (`source:'cam'`) von Peer 1 wird von Peer 2 über `getProducers` mit `kind:video`, `source:cam`, korrektem `userId` gesehen; `closeProducer(gültig)` → Peer 2 erhält `producerClosed`; Leave → `state:null`. **Browser-Test** (Preview): Login, Beitritt (Zuhörer-Modus headless), VoicePanel zeigt Kamera-/Teilen-Buttons, Kamera-Klick scheitert headless sauber (kein Absturz); ein Node-Peer (frieda) produziert einen echten VP8-Kamera-Stream → arians Browser **konsumiert ihn live**: `VoiceStage` rendert eine Kachel „📹 frieda_test · Kamera" mit lebendem Video-Track, Sidebar zeigt 📹; nach Weggang des Peers verschwindet Kachel und Indikator wieder (`producerClosed`-Teardown), Konsole sauber. Build/Tests (32)/Lint/Format grün.
_Hinweis:_ Echte Kamera-/Screen-Aufnahme braucht Geräte (manueller Test) – die Medien-Kontrollebene inkl. Video-Konsum im Browser ist automatisiert/headless vollständig verifiziert (Konsum benötigt keine Kamera).

### Phase 12 – Benachrichtigungen & Einladungen (08.07.2026)

Wie Phase 5–9 kam die Implementierung als angelieferter Commit (`aae0bb7`); diese Session hat reviewt, fünf Lücken geschlossen und die Phase verifiziert.

- **Invites (Prisma `Invite`, Migration `invites_and_push`):** kurzer Code (8× base62, `randomBytes` mit Rejection Sampling gegen Modulo-Bias) → Server; optional Ablauf (1 min–30 Tage) und/oder Nutzungslimit (1–1000). Neues Berechtigungs-Bit **CreateInvite** (1n<<9n); Standardrolle hat es NICHT – einladen darf, wem eine Rolle es gewährt (Owner immer via Administrator)
- **REST** (`apps/api/src/invites/`): `POST/GET /servers/:id/invites` (CreateInvite), `GET /invites/:code` (Vorschau: Name, Mitgliederzahl, Einlader, `alreadyMember` – für jeden Angemeldeten, 404 bei ungültig/abgelaufen/erschöpft), `POST /invites/:code` (Einlösen; Nutzung wird **atomar** verbraucht: `updateMany` mit `uses < maxUses`-Guard, parallele Einlösungen können das Limit nicht überschreiten; Wieder-Einlösen als Mitglied verbraucht nichts), `DELETE /invites/:code` (Ersteller oder ManageServer). Einlösen/Erstellen 20/min, Vorschau 30/min rate-limitiert
- **Offener „Beitritt per Server-ID“ (Phase 3) entfernt** (Route, Store-Methode, „ID kopieren“-Button) – beitreten geht nur noch per Invite-Code, wie in ROADMAP vorgesehen
- **Web-Push (Prisma `PushSubscription`):** Service Worker (`apps/web/public/sw.js`) + VAPID (`web-push`); ohne VAPID-Keys in `.env` sauber deaktiviert. Push geht **nur an Offline-Nutzer** (Presence-Check; online übernimmt die Notification-API aus Phase 9) und ist wegen E2EE **inhaltsarm** (nur Absender/Kanal als Metadaten, kein Klartext). DM-Nachrichten pushen den Partner automatisch; für Server-Kanäle meldet der sendende Client Erwähnte per `POST /channels/:id/notify-mentions` (Server prüft Sende-Berechtigung, filtert auf Kanal-Mitglieder mit ViewChannels). 404/410 vom Push-Dienst räumt die Subscription auf
- **Frontend:** `InviteDialog` (erstellen mit Ablauf/Limit, Liste, Link kopieren, widerrufen), „Server beitreten“-Dialog mit Code/Link-Eingabe + Vorschau, **Deep-Link `/invite/<code>`** (öffnet den Dialog vorbefüllt, säubert die URL), Push-Schalter auf der Profilseite (Status unsupported/denied/enabled), `resyncPush` nach Login, Unsubscribe beim Logout
- **Review-Fixes dieser Session:** (1) offenen Join-Endpunkt entfernt (s. o. – jeder mit Server-UUID hätte weiter beitreten können); (2) Push-Endpoint-Validierung: nur **https**-URLs + Längenlimits (sonst API als Relay gegen beliebige/interne HTTP-Ziele missbrauchbar – SSRF); (3) Deckel 10 Subscriptions pro Nutzer (älteste fliegen raus – vorher unbegrenztes DB-Wachstum); (4) Rate-Limit auf die Invite-Vorschau (Code-Enumeration); (5) Modulo-Bias in der Code-Generierung beseitigt (Rejection Sampling)

**Verifiziert:** Skript mit **44 Checks, alle grün** – Invite-Lebenszyklus (Erstellen nur mit Recht: Nicht-Mitglied 404/Mitglied ohne Recht 403; DTO-Grenzen; Vorschau inkl. 404-Fälle; Einlösen inkl. Doppel-Einlösung ohne Verbrauch; maxUses=1 sequenziell UND parallel – genau eine Einlösung gewinnt; Ablauf via DB-Manipulation; Widerruf-Rechte; alter Join-Endpunkt 404; Vorschau-Rate-Limit 429). Web-Push **mit echter Zustellung**: lokaler HTTPS-Push-Receiver (selbstsigniertes Zertifikat + `NODE_EXTRA_CA_CERTS`), gültige P-256-Subscription-Schlüssel → verschlüsselter Push (aes128gcm, kein Klartext im Body) kommt beim Offline-Empfänger an; 410-Endpoint wird aus der DB geräumt; **Online-Empfänger bekommt KEINEN Push** (Presence-Gating, per Gateway-WS verifiziert); Erwähnungs-Push filtert Nicht-Mitglieder still; http-Endpoint 400; Deckel greift (12 → 10); Unsubscribe räumt DB. UI-Test (Preview): Owner erstellt Invite im Dialog (Liste, Ablauf-Anzeige, Link-Button), zweiter frisch registrierter Nutzer folgt dem **Deep-Link** `/invite/<code>` → Dialog mit Vorschau („1 Mitglied · eingeladen von …“) → Beitritt → Server + Mitglieder-Panel (2) live; ohne CreateInvite kein „Einladen“-Button; Profilseite zeigt Push-Sektion (headless sauber „blockiert“-Fallback); Konsole sauber. Build/Tests (32)/Lint/Format grün.

### Phase 13 – Moderationswerkzeuge (09.07.2026)

Selbst gebaut (kein angelieferter Commit) auf der bestehenden REST-/Gateway-/SFU-Infrastruktur. Kern: serverseitig durchgesetzte Moderation mit **Rollen-Hierarchie-Schutz** – moderieren darf man nur Mitglieder mit strikt niedrigerem Rang (höchste Rollen-Position; Owner = ∞), nie den Owner, nie sich selbst.

- **Rechte (`packages/shared/src/permissions.ts`):** zwei neue Bits `ModerateMembers` (1<<10, Timeout + Voice-Trennen) und `ViewAuditLog` (1<<11); `KickMembers`/`BanMembers`/`ManageMessages` aus Phase 5 sind jetzt aktiv. Unbekannte Bits maskiert die Rollen-Verwaltung weiterhin weg
- **Prisma (Migration `moderation`):** `Ban` (serverId+userId, bannedById, reason), `AuditLogEntry` (actor/action/target + denormalisiertes `targetUsername`, damit Einträge lesbar bleiben, wenn das Ziel den Server verlässt), `Membership.timeoutUntil`
- **Moderation-Modul (`apps/api/src/moderation/`):** `POST …/members/:id/kick`, `PUT/DELETE …/bans/:id` (+ `GET …/bans`), `PUT/DELETE …/members/:id/timeout`, `POST …/members/:id/voice-disconnect`, `GET …/audit-log`. Hierarchie zentral in `assertCanModerate` (`PermissionsService.getMemberRank`), jede Aktion schreibt einen Audit-Eintrag
- **Durchsetzung:** Bann sperrt (Neu-)Beitritte inkl. Invite-Einlösung (`ServersService.assertNotBanned`, VOR Nutzungsverbrauch geprüft). Timeout blockiert Senden/Uploads (`ChannelAccessService`) und Voice-Beitritt (`VoiceService.join`), Lesen bleibt erlaubt; ein Timeout trennt zusätzlich aus dem Voice
- **Voice-Moderation:** `VoiceService.forceDisconnect` publiziert `voice:force-disconnect` → der SFU (`apps/voice`) abonniert den Kanal und schließt die Medien-WS (Close-Code 4006); der Client räumt über den bestehenden `onClosed`-Pfad auf
- **Nachrichten bearbeiten/löschen (`MessagesService`):** `PATCH`/`DELETE /api/channels/:id/messages/:id` – Autor bearbeitet/löscht eigene, ManageMessages löscht fremde in Server-Kanälen (Audit-Eintrag), gebundene MinIO-Blobs werden mit entfernt; Gateway-Events `MESSAGE_UPDATE`/`MESSAGE_DELETE`. Bearbeiten läuft weiter E2EE (Client verschlüsselt den Text neu und erhält Anhänge/Zitat)
- **Gateway:** neue Events `MESSAGE_UPDATE`, `MESSAGE_DELETE`, `SERVER_MEMBER_UPDATE` (Timeout gesetzt/aufgehoben); Kick/Bann nutzen `SERVER_MEMBER_REMOVE`
- **Frontend:** Moderations-Menü (⋯) im Mitglieder-Panel (Kicken/Bannen/Auszeit mit Dauer-Presets/Auszeit aufheben/aus Voice trennen), ⏳-Auszeit-Indikator, 🛡️-`ModerationDialog` (Bannliste mit Entbannen + Audit-Log), Inline-Bearbeitung + „(bearbeitet)“-Hinweis und 🗑️-Löschen in `MessageRow`; Stores um Moderations-Aktionen und die neuen Events erweitert. UI blendet nur aus – durchgesetzt wird serverseitig

**Verifiziert:** Skript mit **62 Checks, alle grün** (echte API + Gateway + echter SFU): Rechte-Gating (Nicht-Mitglied 404 / Mitglied ohne Recht 403), Hierarchie (Selbst 400, Owner 403, Höherrangiger 403), Kick (Broadcast an Mitglieder + Betroffenen, Rückkehr per Invite), Bann (Bannliste mit Grund/Banner, Invite-Einlösung 403, Entbannen), Timeout (Senden 403 / Lesen 200 / Voice-Beitritt 403, `SERVER_MEMBER_UPDATE`, DTO-Grenzen 0/Max → 400), Nachricht bearbeiten (nur Autor, `MESSAGE_UPDATE`, Fremd-Edit 403, Außenstehender 404) + löschen (Mod via ManageMessages → Audit, Mitglied ohne Recht 403, Anhang-Blob weg → 404), Audit-Log (alle 7 Aktionstypen + Actor/Target/Grund, ohne ViewAuditLog 403), Voice-Force-Disconnect (Medien-WS-Close 4006, Roster geräumt, zweiter Aufruf 404, Audit `VOICE_DISCONNECT`). Browser-Test (Preview): Owner schickt Mitglied per ⋯-Menü in Auszeit → ⏳ live; `ModerationDialog` zeigt Audit-Log; Bannen entfernt das Mitglied live und listet den Bann (mit Grund) → Entbannen leert die Liste; eigene Nachricht senden (E2EE) → bearbeiten („bearbeitet“) → löschen; Konsole sauber. Build/Tests (11 API + 21 shared)/Lint/Format grün

### Phase 14 – Skalierungs-Härtung (09.07.2026)

Wie Phase 5–9/12 kam die Implementierung als angelieferter Commit (`5454690`, „Frank"); diese Session hat reviewt, drei Lücken geschlossen, die DB-Indizes geprüft und die Phase gegen echte Infrastruktur verifiziert. Fokus der Phase: **Korrektheit horizontaler Skalierung** für Gateway (Presence) und Voice (Roster) plus `trust proxy` – bewusst KEINE Lasttests (brauchen ein deployed Multi-Instanz-Setup) und keine großen Umbauten (presigned Uploads, Message-Queue) – die bleiben in der ROADMAP.

- **Presence gezielt statt global (`presence.service.ts`):** neues `filterOnline(candidateIds)` prüft NUR den Sichtbarkeitskreis gegen den Verbindungszähler (EXISTS-Pipeline + ein HMGET) – O(Kreis) statt O(alle Online). Alle drei Aufrufer (READY-Snapshot in `gateway.service`, Server-Beitritt in `servers.service`, neue Freundschaft in `friends.service`) umgestellt; das alte `getOnlineUsers()` (HGETALL über ALLE) ist damit entfernt
- **`trust proxy` (`main.ts`):** über `TRUST_PROXY` in `.env` konfigurierbar (Hop-Zahl wie `1` oder Express-Ausdruck); leer im Dev. Gesetzt kommt `req.ip` aus X-Forwarded-For → Rate-Limit pro echter Client-IP statt gemeinsam unter der Proxy-IP
- **Multi-instanz-fester Voice-Roster (`voice.service.ts` + `apps/voice`):** ersetzt das destruktive „beim API-Start ALLE VoiceSessions löschen" (riss in Multi-Instanz die Sessions noch verbundener Nutzer anderer Instanzen mit). Der SFU setzt jetzt pro verbundenem Peer einen Redis-Liveness-Key `voice:peer:{userId}` (Wert = channelId, TTL 30 s, 10-s-Heartbeat, sofort beim Welcome); die API gleicht den Roster beim Boot UND alle 30 s ab und entfernt Sessions ohne lebenden Key (mit 30-s-Beitritts-Karenz gegen das Zeitfenster join↔Welcome). `voice:disconnect` bleibt der schnelle Pfad, der Abgleich ist die Absicherung gegen verpasste Signale/SFU-Absturz
- **DB-Index-Review:** Hot-Paths sind bereits abgedeckt (Message `[channelId,createdAt]`, AuditLog `[serverId,createdAt]`, Membership/Channel/Role/Invite/Push je `serverId`/`userId`, KeyEnvelope `[toUserId,createdAt]`, Voice `channelId` + unique `userId`) – **keine neue Migration nötig**
- **Review-Fixes dieser Session:** (1) `filterOnline` räumt jetzt auch die Presence-Hash-Leichen im geprüften Kreis lazy auf (das war Aufgabe des entfernten `getOnlineUsers` – sonst wäre der `presence:users`-Hash bei harten Instanz-Abstürzen unbegrenzt gewachsen) und das tote `getOnlineUsers` ist weg; (2) die Fire-and-forget-Redis-Schreibvorgänge im SFU (`markPeerAlive` jetzt alle 10 s, `publishDisconnect`) haben ein `.catch` bekommen – ein Redis-Aussetzer nach `maxRetriesPerRequest` hätte sonst eine unbehandelte Promise-Rejection geworfen und den SFU beenden können; (3) toter `channel`-Include im Voice-Reconcile entfernt

**Verifiziert:** Skript mit **19 Checks, alle grün** (echtes Redis + echter SFU + echte API/DB, alle Dienste vom Skript selbst gestartet/gestoppt). [A] echter `PresenceService.filterOnline`: leere Eingabe → [], nur Online-Kandidaten zurück (Offline/Nie-Registrierte nicht), Username mitgeliefert, dedupliziert, Hash-Leiche wird NICHT zurückgegeben UND aus `presence:users` entfernt, nach `markOffline` weg. [B] echter SFU mit gefälschtem gültigem Voice-Token: Welcome nach auth, `voice:peer:{user}` mit Wert=channelId und TTL ≤ 30 gesetzt, nach 12 s durch den Heartbeat wieder auf ≥ 22 erneuert, nach WS-Trennung noch da (läuft per TTL ab statt sofort als Zombie). [C] echte API: drei seed-Sessions in einem VOICE-Kanal – verwaiste alte (kein Key) wird vom **Boot-Reconcile entfernt**, frische (Karenz) und lebende alte (Key gesetzt) bleiben; Reconcile-Logzeile bestätigt. Build (4 Workspaces)/Tests (11 API + 21 shared)/Lint/Format grün.

### Phase 15 – Feinschliff (in Arbeit, 09.07.2026)

Selbst gebaut, inkrementell (kleine verifizierte Schritte). Erledigt bisher:

- **Performance – Code-Splitting des Voice-Bundles:** `store/voice.ts` importiert `VoiceClient`/`VideoTile` nur noch als Typ und lädt das Voice-Modul per dynamischem `import('../lib/voice')` erst beim ersten Sprachkanal-Beitritt (parallel zum Join-Request). Rollup zieht mediasoup-client dadurch in einen eigenen Chunk – das **Initial-Bundle sank von 975 KB auf 736 KB (gzip 285 → 242 KB)**, der Voice-Chunk (239 KB / 43 KB gzip) lädt nur bei Bedarf. **Verifiziert im Browser** (api+voice+web live, echter SFU): vor Beitritt ist weder `lib/voice` noch `mediasoup-client` geladen; Klick auf einen Sprachkanal lädt beide nach, Voice verbindet („Sprache verbunden", Self im Roster, Zuhörer-Modus headless), Konsole sauber. Build/Tests (11 API + 21 shared)/Lint/Format grün

**Schritt 2 (10.07.2026):** Wie Phase 5–9/12/14 kamen die UI-Feinschliff-Punkte als angelieferte Commits (`7c8fb44` „Herp", `e94b25c` „fertig"); diese Session hat reviewt, vier Lücken geschlossen und alles verifiziert.

- **DM-Ungelesen-Badge:** `store/dms.ts` zählt live eingetroffene fremde DM-Nachrichten pro Kanal (`unread`), solange die DM-ChatView nicht sichtbar ist (`activeDmId`, von der ChatView gemeldet); Badges in der DM-Liste (`HomeView`) und aufsummiert auf dem Zuhause-Button (`ServerRail`, zusammen mit Freundschaftsanfragen, „9+"-Deckel). Gezählt wird erst NACH dem Entschlüsseln (Reaktions-Events erhöhen nichts); nur im Speicher (kein Ungelesen-Stand über Reloads – bewusst, wie die In-App-Notifications)
- **Rollenfarben:** Farb-Palette (10 Töne + „Keine Farbe") im `RolesDialog` (nicht für die Standardrolle); `UpdateRoleDto.color` akzeptiert Hex oder `null` (= entfernen); `memberRoleColor` (höchste Rolle mit Farbe, wie Discord) färbt Namen im Mitglieder-Panel UND Absendernamen im Chat (`MessageRow.senderColor`)
- **Erwähnungs-Notification für fremde Server:** `MESSAGE_CREATE` trägt bei Server-Kanälen jetzt `context` (Kanal- + Servername – reine Server-Metadaten, kein E2EE-Inhalt; bei DMs entfällt er). Der Client merkt sich die Labels (`rememberChannelLabel`) und die Notification nennt statt „… in einem Kanal" nun z. B. „#allgemein (P15 Zweit)". Nebenbei: `pushDirectMessage` nutzt dieselbe Kanal-Abfrage (kein Mehr-Query)
- **Zitat-Original-Nachladen:** „Zum Original springen" lädt fehlende History rückwärts nach (Deckel 10 Seiten à 50), scrollt hin und lässt die Nachricht aufleuchten; klare Fehlertexte („nicht in den letzten 500" / „vermutlich gelöscht")
- **⏳-Auszeit-Timer:** `MembersPanel` berechnet das früheste Auszeit-Ende und rendert per Timeout neu (+500 ms, 2^31-Klemmung) – das ⏳ verschwindet beim Ablauf ohne Reload/Refetch
- **Layout-Politur:** `html/body/#root` overflow:hidden, Chat-Bereich mit `min-h-0`-Kette und dezenter Hover-Scrollbar (`chat-scrollbar`)
- **Review-Fixes dieser Session:** (1) `pendingLive` (Live-Nachzügler, deren Entschlüsselung das Rennen gegen den Schlüssel-Umschlag verliert) merkt sich jetzt die ganze `MessageInfo` statt nur der ID – Nachrichten NIE geöffneter Kanäle (Normalfall bei DMs!) landen nicht in `byChannel`, `retryUndecrypted` hätte sie sonst nie nachgeholt und Badge/Notification wären genau beim wichtigsten Fall (erste Nachricht eines neuen DM-Partners) verloren gegangen; außerdem wird die Map bei `reset()` geleert (sonst gälten nach Re-Login History-Nachrichten als „live"); (2) Standardrolle ist auch SERVERSEITIG farblos (`color` wird wie `name`/`position` bei `isDefault` ignoriert – die UI blendete nur aus); (3) Scroll-Regression durch das globale overflow:hidden behoben: Auth-/Profilseite nutzen `h-full overflow-y-auto` + `m-auto`-Zentrierung statt `min-h-screen items-center` (Inhalt höher als der Viewport wurde sonst abgeschnitten); (4) `Modal` bekam `max-h-full overflow-y-auto` (hohe Dialoge – z. B. Rollen mit Palette – scrollen intern statt zu klippen)

**Verifiziert (Schritt 2):** API-Skript mit **12 Checks, alle grün** (zwei frische Nutzer, echte API + Gateway + E2EE im Skript): `MESSAGE_CREATE` mit `context` (Kanal-/Servername) im Server-Kanal und OHNE `context` in der DM, Empfänger entschlüsselt beides; Rollenfarben-API (setzen 200 + `ROLE_UPDATE`-Event trägt Farbe, `rot`/`#12345` → 400, `null` entfernt, Standardrolle bleibt trotz PATCH farblos). Browser-Interop (Preview, arian UI ↔ frieda Node-Skript): UI-Nachricht wird vom Skript entschlüsselt (inkl. context), Skript-Antwort erscheint lesbar; Rolle „Bunt" per Palette rot gefärbt + frieda zugewiesen → Name in Mitglieder-Panel UND Chat `rgb(231,76,60)`; DM vom Skript → Badge „1" auf Zuhause-Button und DM-Liste, Öffnen leert ihn, zweite DM bei offener DM erhöht nichts; 60-s-Auszeit → ⏳ erscheint live und verschwindet nach Ablauf **ohne einen einzigen Fetch** (fetch-Log instrumentiert); Erwähnung aus dem NICHT ausgewählten Zweitserver → Notification-Titel „… erwähnt dich in #allgemein (P15 Zweit)" (Notification-API gestubbt); Zitat auf eine 53 Nachrichten alte Nachricht nach Reload (nur 50 geladen) → Klick lädt nach (50 → 55), Original im Viewport; Profilseite bei 420 px Höhe scrollbar bis zum Abmelden-Button; Rollen-Modal scrollt intern; Konsole über die gesamte Session sauber. Build (4 Workspaces)/Tests (11 API + 21 shared)/Lint/Format grün

**Schritt 3 (10.07.2026) – Voice-/Video-UI:** Selbst gebaut (kein angelieferter Commit), rein clientseitig (kein API-/SFU-Umbau):

- **Sprech-Indikator:** `SpeakingDetector` in `lib/voice.ts` – WebAudio-Pegelmessung (AnalyserNode, RMS über dem Zeitsignal, Schwelle ≈ −36 dBFS, 500 ms Haltezeit gegen Flackern, Poll alle 120 ms) auf dem eigenen Mikro-Track und jedem fremden Audio-Consumer; ein gemeinsamer `AudioContext` pro VoiceClient (Autoplay-Policy: Resume-Nachholung beim nächsten Klick/Tastendruck, falls die Beitritts-Geste durch die async-Kette verfallen ist). Meldet `onSpeakingChanged(userId, bool)` → `speaking`-Map im Voice-Store. UI: grüner Ring um den Avatar im Sprachkanal-Roster (Sidebar) und um die Video-Kacheln des Sprechers. Eigene Stummschaltung setzt die Erkennung explizit aus (`setEnabled(false)` – der pausierte Producer stoppt den lokalen Track nicht). Aufräumen überall: Consumer-Ende stoppt den Detector (meldet vorher false), Disconnect schließt den AudioContext, Store leert `speaking` bei Join/Leave/Close/Reset
- **Video-Bühnen-Layout (`VoiceStage`):** Läuft eine Bildschirmfreigabe, wird sie automatisch groß dargestellt (fremde vor der eigenen, `h-[40vh]`), alle übrigen Streams wandern in eine horizontale Thumbnail-Leiste (h-20, scrollbar); jede Kachel ist klickbar: anpinnen (📌 im Label, Titel „Fokus lösen“) bzw. Pin lösen → Auto-Regel greift wieder; verschwindet die angepinnte Kachel, löst sich der Pin selbst. Ohne Screen-Share und ohne Pin: das bisherige 1–3-Spalten-Raster (dort pinnt ein Klick ebenfalls). Kachel-Label in der Leiste nur noch Name (Platz), `truncate` gegen absenderkontrolliert lange Namen

**Verifiziert (Schritt 3, Browser gegen echten SFU + API, frieda als Node-Peer mit fabrizierten Producern wie Phase 11):** frieda produziert mic (Opus) + cam (VP8) + screen (VP8) ohne echte Medien → Sidebar-Roster zeigt sie mit 🖥️📹 (live); arian tritt bei (headless Zuhörer-Modus) und **konsumiert real** (lebende Video-Tracks): Bühne fokussiert den Bildschirm automatisch groß (704×288 = 40vh) mit Kamera-Thumbnail (142×80) darunter; Klick aufs Thumbnail pinnt die Kamera groß (📌, „Fokus lösen“), Klick auf die große Kachel löst den Pin → Bildschirm wieder fokussiert. Sprech-Erkennung: echter `SpeakingDetector` (dieselbe Klasse wie am Consumer) mit synthetischem Oszillator-Track im Browser – 1 s Stille → kein Event, Ton an → `true` nach ~80 ms, Ton aus → `false` nach der Haltezeit (~600 ms); Store→UI-Verdrahtung geprüft (speaking für frieda → grüner Ring an Bühnen-Kacheln UND Sidebar-Avatar, Rücknahme entfernt alle Ringe); friedas stiller echter Consumer erzeugt KEIN false-positive. Abbau: Peer-Kill → Kacheln/Roster/`<audio>`-Elemente verschwinden live, `speaking` leer; Trennen-Button räumt vollständig (status idle). Konsole über die gesamte Session fehlerfrei, SFU-Log ohne Fehler. Build (4 Workspaces)/Tests (11 API + 21 shared)/Lint/Format grün. _Grenze (dokumentiert):_ Sprech-Erkennung Ende-zu-Ende mit echtem RTP-Audio braucht ein echtes Mikrofon (manueller Test) – der fabrizierte Producer sendet keine Medien; verifiziert sind Erkennungslogik (synthetischer Track durch dieselbe Klasse), Verdrahtung und Negativfall am echten Stream

**Schritt 4 (11.07.2026) – Onboarding-UI:** Selbst gebaut, rein clientseitig (kein API-Umbau):

- **Willkommens-Ansicht (`WelcomeView`, neu):** erscheint in der Home-Ansicht statt des Freunde-Panels, wenn das Konto komplett frisch ist (alle drei Stores geladen UND leer: keine Server, keine Freunde/Anfragen/Blockierte, keine DMs – wer schon interagiert hat, ist nicht frisch). Begrüßung mit Benutzernamen, drei Aktionskarten (📨 Einladung einlösen → Beitritts-Dialog, 🚀 Eigenen Server erstellen → Erstell-Dialog, 👥 Freunde hinzufügen → Freunde-Panel), 🔒-E2EE-Hinweis, „Überspringen“. Skip lebt nur im Speicher (wie die Ungelesen-Zähler) – die Ansicht verschwindet dauerhaft von selbst, sobald eine Aktion Früchte trägt (Bedingung ist reaktiv, greift auch rückwärts nach Server-Löschung). `MainPage` reicht die Dialog-Handler an `HomeView` durch
- **Kleinpolitur:** veralteter Tooltip „Server beitreten (per ID)“ → „(mit Einladung)“ (Beitritt läuft seit Phase 12 über Invite-Codes); Empty-Hinweis „Noch keine Textkanäle“ in der Kanal-Sidebar (Sprachkanäle hatten ihn schon)
- **Edge-Case-Fix (beim Verifizieren gefunden):** Der `CHANNEL_DELETE`-Fallback im Servers-Store wählte `channels[0]` ohne Typ-Filter – nach dem Löschen des letzten TEXTkanals (bei vorhandenem Sprachkanal) zeigte die ChatView den SPRACHkanal als Text-Chat („Nachricht an #talk“). Jetzt fällt die Auswahl nur auf Textkanäle zurück (sonst null → „Wähle einen Kanal“); `selectServer` und `CHANNEL_CREATE` hatten den Filter bereits

**Verifiziert (Schritt 4, Browser gegen echte API, frischer Nutzer `onboard_p15` über die UI registriert):** Willkommens-Ansicht erscheint direkt nach der Registrierung (Begrüßung mit Namen, drei Karten, E2EE-Hinweis, aktualisierter Rail-Tooltip); Karte „Einladung einlösen“ öffnet den Beitritts-Dialog, „Eigenen Server erstellen“ den Erstell-Dialog → Server angelegt, landet direkt im Server mit #allgemein; zurück auf Zuhause ist die Ansicht weg (Konto nicht mehr frisch), nach Server-Löschung reaktiv wieder da; „Freunde hinzufügen“ wechselt zum Freunde-Panel; Reload bringt die Ansicht zurück (Skip nur im Speicher), nach ausgehender Freundschaftsanfrage bleibt sie auch nach Reload weg; Edge-Case-Fix geprüft (Server nur mit Sprachkanal: vor dem Fix zeigte die ChatView den Sprachkanal als Text-Chat – reproduziert; nach dem Fix „Wähle einen Kanal“ + Sidebar „Noch keine Textkanäle“); Konsole über die gesamte Session sauber. Build (4 Workspaces)/Tests (11 API + 21 shared)/Lint/Format grün

**Schritt 5 (11.07.2026) – Fehlerbehandlung/Edge-Cases + ROADMAP-Finalisierung (Phase 15 abgeschlossen):** Selbst gebaut.

- **„Du wurdest gekickt/gebannt“-Rückmeldung (ROADMAP-Punkt aus Phase 13):** Neues Gateway-Event `SERVER_SELF_REMOVED` (Servername, Anlass kick/ban, Grund) – geht NUR an den Betroffenen (`ModerationService.notifySelfRemoved`); der Broadcast `SERVER_MEMBER_REMOVE` an die übrigen Mitglieder bleibt bewusst ohne Moderationsdetails. Der Servername reist im Payload mit, weil der Client den Server beim Eintreffen bereits vergessen hat. Client: neuer Hinweis-Store (`store/notices.ts`, Queue) + `NoticeHost` (Modal mit OK, in `App` gerendert – ansichtsunabhängig), Reset beim Logout
- **ErrorBoundary (`components/ErrorBoundary.tsx`):** Render-Fehler zeigen statt einer weißen Seite eine Fallback-UI (Meldung + Fehlertext + „Neu laden“); um `<App/>` in `main.tsx`
- **Unbehandelte Promise-Rejections beseitigt:** READY-Reloads (`loadServers/loadFriends/loadDms`) und `FRIENDS_UPDATE` loggen Fehler jetzt statt unbehandelt zu rejecten (API kann nach Reconnect kurz weg sein; der nächste READY lädt ohnehin); `ChatView.loadHistory`/`loadOlder` zeigen die Fehlerzeile; Server-/Kanal-Aktionen der Sidebar (löschen/verlassen) melden Fehler als Hinweis-Dialog (`runAction`)
- **E2EE-Init-Race gefixt (beim Verifizieren live gefunden):** `e2ee.init` schrieb die Modul-Globals schon während der await-Kette – ein paralleles `reset()` (Logout, React-StrictMode-Remount der Gateway-Verbindung) setzte `db` auf null und der laufende Init griff danach darauf zu (`TypeError … reading 'get'`, bei jedem Dev-Load in der Konsole). Jetzt: lokal arbeiten, am Ende nur committen, wenn der Versuch noch aktuell ist (`initPromise`-Identität); veraltete Versuche schließen ihre DB und verwerfen das Ergebnis
- **ROADMAP.md finalisiert:** Status-Kopf „Phasenplan abgeschlossen“ mit den drei Blöcken „vor echtem Deployment / vor Mehrbenutzer-Betrieb / vor öffentlichem Launch“; erledigte Punkte markiert

**Verifiziert (Schritt 5, Browser gegen echte API + portable Infra):** Frischer Nutzer `p15opfer` (UI) + Owner-Skript `p15mod_…` (REST): Beitritt per Invite → Kick mit Grund → Hinweis-Dialog „Entfernt … Grund: Zu viel Spam im Testkanal“ erscheint live, Server verschwindet aus der Leiste; erneuter Beitritt → Bann mit Grund → Dialog „Gebannt … Grund: Wiederholter Regelverstoss“; erneuter Invite-Einlöse-Versuch → serverseitig blockiert („Du bist von diesem Server gebannt“ im Dialog). ErrorBoundary: temporärer `throw` in `WelcomeView` → Fallback-UI mit Fehlertext und „Neu laden“ statt weißer Seite (danach entfernt, App normal). E2EE-Regression nach dem Init-Umbau: neue Nachricht in frischem Server wird verschlüsselt gesendet (DB: nur Ciphertext, kein Klartext-Fund), übersteht Reload lesbar; Konsole in frischem Tab komplett sauber (der Init-TypeError trat vorher bei jedem Load auf). Build (4 Workspaces)/Tests (11 API + 21 shared)/Lint/Format grün.

### Deployment: VPS via GitHub Actions + GHCR (11.07.2026, nach Phase 15)

Auf Arians Wunsch: automatisches Deployment auf seinen VPS (dort läuft bereits eine andere Website; Setup so gebaut, dass sie unberührt bleibt).

- **Dockerfiles** für alle drei Dienste (Build-Kontext = Repo-Root wegen npm-Workspaces): `apps/api` (Prisma generate → nest build → `npm prune --omit=dev` → generate erneut, da prune `.prisma` entfernt; Start via `prisma migrate deploy && node dist/main.js`), `apps/voice` (mediasoup, vorgebauter Worker), `apps/web` (Vite-Build → nginx:alpine). Dazu `.dockerignore` (wichtig: `**/*.tsbuildinfo`, sonst überspringt tsc den Emit)
- **`prisma` (CLI) ist jetzt Prod-Dependency der API** – das Laufzeit-Image braucht sie für `migrate deploy` beim Containerstart
- **`apps/web/nginx.conf`:** Web-Container ist einziger HTTP-Einstiegspunkt – SPA + Proxy auf `/api`, `/gateway`, `/voice` (WebSocket-Upgrade via `map $http_upgrade`), `client_max_body_size 12m`, Cache-Header (index.html no-cache, /assets immutable)
- **`infra/docker-compose.prod.yml`:** Postgres/Redis/MinIO nur im internen Netz, Web an `${WEB_BIND:-127.0.0.1:8080}`, Voice veröffentlicht die RTC-Range UDP/TCP 40000–40100 1:1, Healthchecks über `node -e "fetch(...)"`; Vorlage `infra/.env.production.example`
- **`.github/workflows/deploy.yml`:** Push auf `main` (oder manuell) → 3 Images per Matrix bauen → GHCR (`ghcr.io/8nkmjygvzc-maker/parley-{api,voice,web}`, Tags `latest` + Git-SHA, GHA-Layer-Cache) → per SSH (appleboy-Actions) compose-Datei nach `~/parley/` kopieren, `docker compose pull && up -d`. Secrets: `VPS_HOST`, `VPS_USER`, `VPS_SSH_KEY`, optional `VPS_PORT`; GHCR-Pull auf dem VPS läuft über das Workflow-`GITHUB_TOKEN`
- **`DEPLOY.md`:** komplette VPS-Anleitung (Bestandsaufnahme wegen der bestehenden Website, .env/Secrets, Firewall für die RTC-Ports, DNS, Reverse-Proxy-Varianten nginx/Proxy-Container/Caddy, Rollback über `IMAGE_TAG`=SHA, Backup-Hinweise)
- **`.gitignore` bereinigt:** Datei enthielt committete Merge-Konflikt-Marker (`<<<<<<< HEAD` …) – neu zusammengeführt; `!.env.production.example` ergänzt, damit die Vorlage nicht vom `.env.*`-Muster ignoriert wird

### Feature: Soundboard (12.07.2026, nach Phase 15)

Auf Wunsch von Rijon: ein Discord-artiges Soundboard – jeder Server hat eine Sound-Bibliothek (Name + Emoji + Lautstärke + Audiodatei, **Anzahl unbegrenzt**, nur 1 MiB pro Datei), wer in einem Sprachkanal sitzt, spielt Sounds für alle Teilnehmer ab.

- **Mechanik (wie Discord, kein SFU-Umbau):** Abspielen ist ein REST-Aufruf (`POST /voice/channels/:id/soundboard/:soundId/play`); die API prüft Rechte + eigene aktive VoiceSession in GENAU diesem Kanal und verteilt `SOUNDBOARD_PLAY` per Gateway **nur an Nutzer, die gerade im Kanal sitzen**. Jeder Client spielt den (pro Sound-ID als Object-URL gecachten) Blob lokal ab – nichts wird in den SFU gemischt. Deafen unterdrückt die Wiedergabe clientseitig
- **Rechte:** zwei neue Bits – `UseSoundboard` (1<<12, abspielen; Teil der Standardrolle wie bei Discords @everyone, Bestands-Server per Backfill in der Migration versorgt) und `ManageSoundboard` (1<<13, hochladen/umbenennen/löschen); eigene Uploads darf der Uploader immer löschen. Auszeit (Phase 13) blockiert Upload UND Abspielen
- **API (`apps/api/src/soundboard/`):** `GET/POST /servers/:id/soundboard` (Upload wie Phase 8 als roher `application/octet-stream`-Body, Metadaten als Query-Parameter: Name ≤ 32, Emoji via `isPlausibleReactionEmoji`, Volume 0.05–1, MIME nur `audio/*`), `PATCH/DELETE …/:soundId`, `GET /soundboard/:id/audio` (Stream, nur Mitglieder), Play-Route; Events `SOUNDBOARD_UPDATE` (an Mitglieder mit ViewChannels) und `SOUNDBOARD_PLAY`. Prisma-Modell `SoundboardSound` (Migration `soundboard`, objectKey `soundboard/{serverId}/{uuid}`), Blob-Aufräumen bei Sound- und Server-Löschung (`removeAllWithPrefix`)
- **Dauer-Begrenzung ohne Server-Dekodierung:** Der Server prüft nur die Größe; ehrliche Clients dekodieren die Datei VOR dem Upload (`OfflineAudioContext`, max. 10 s) und JEDER Empfänger deckelt die Wiedergabe hart auf 12 s (`MAX_SOUNDBOARD_PLAY_SECONDS`) – manipulierte überlange Uploads laufen damit ins Leere (Trade-off in ROADMAP.md)
- **Web:** 🎶-Button im `VoicePanel` öffnet den `SoundboardDialog` (Grid, Klick = für alle abspielen, Hover: ✏️/🗑️; mit ManageSoundboard Upload-Formular mit Datei/Name-Vorschlag/Emoji/Lautstärke-Slider und Inline-Bearbeitung); flüchtige „🔊 X spielt Y“-Anzeige im Panel UND Dialog; Stores `store/soundboard.ts` + `activeServerId` im Voice-Store (man kann während der Voice-Verbindung einen anderen Server ansehen); Sounds sind **unverschlüsselte Server-Assets** (bewusst, siehe ROADMAP)
- **Verifiziert:** Skript mit **35 Checks, alle grün** (echte API + Gateway + MinIO, 4 Nutzer): Standardrolle hat UseSoundboard (nicht ManageSoundboard); Upload-Gating (ohne Recht 403, Fremde 404, text/plain / leer / >1 MiB / Text-„Emoji“ → 400, keine Anzahl-Grenze, kein objectKey-Leak); Audio-Download nur für Mitglieder (Bytes identisch, korrekter MIME); Play verlangt Voice-Session (400) + UseSoundboard (Entzug → 403, serverseitig) + keine Auszeit (403), Sound fremder Server 404; **`SOUNDBOARD_PLAY` erreicht genau die Kanal-Insassen** (Mitglied außerhalb bekommt nichts); PATCH/DELETE-Rechte inkl. Uploader-Selbstlöschung ohne ManageSoundboard; nach Löschen Audio 404; Server-Löschung räumt Blobs. **Browser-Test** (Preview, api+voice+web): Beitritt in Sprachkanal (Zuhörer-Modus), 🎶 öffnet Dialog mit geseedetem Sound; Klick → Play-POST 204 → `SOUNDBOARD_PLAY` kommt zurück → Client lädt und spielt den Blob; Zweitnutzer (Skript) spielt ab → Banner „🔊 sbui_bot…: 🎺 Tada“ erscheint live (per DOM-Beobachter belegt) und der Browser holt das Audio; **Upload über die UI** mit synthetischer WAV (echte Dekodierung + Dauer-Prüfung im Browser) → 201, Name-Vorschlag aus dem Dateinamen, Sound erscheint im Grid; Umbenennen per ✏️ (Grid aktualisiert), Löschen per 🗑️; „Soundboard verwenden/verwalten“ im Rollen-Dialog; Konsole über die gesamte Session fehlerfrei. Build (4 Workspaces)/Tests (11 API + 21 shared)/Lint/Format grün

### Bugfix-/Feature-Runde nach Arians Website-Test (12.07.2026)

Arian hat die Website getestet und 8 Punkte gemeldet; alle in dieser Session umgesetzt und lokal im Browser verifiziert. (Hinweis: Die Session wurde einmal komplett wiederholt, weil der erste Durchlauf vor einem `git pull` entstand und beim Pull verworfen wurde – der zweite Durchlauf setzt auf dem Soundboard-Stand auf.)

- **Voice-Rauswurf-Bug gefixt:** Klick auf den bereits verbundenen Sprachkanal rief `joinVoice` erneut auf; die neue SFU-Verbindung ersetzte die alte, deren `onClosed`-Handler räumte Session + Roster ab („man fliegt raus"). Fix: Early-Return in `store/voice.ts`, wenn man schon in diesem Kanal ist (außer im Fehlerzustand)
- **Bild-/Datei-Versand-Bug gefixt (Ursache gefunden):** `input.files` ist eine **LIVE-FileList** – der onChange-Handler leerte den Input (`e.target.value = ''`) direkt nach `addFiles`, aber Reacts State-Updater kopierte die Liste erst NACH dem Handler → Auswahl war da bereits leer, Anhänge kamen nie im Entwurf an (betraf Server-Kanäle UND DMs gleichermaßen; die von Arian beobachtete Rollen-Abhängigkeit war Zufall – Server/API waren korrekt, per REST-Skript nachgewiesen). Fix: `Array.from(selected)` synchron VOR dem State-Update (`ChatView.addFiles`)
- **Status für alle sichtbar:** `ServerMember` trägt jetzt `status` + `avatarUrl` (Prisma-Selects + Mapper in servers/moderation); neues Gateway-Event **`USER_UPDATE`** (an Sichtbarkeitskreis + Selbst) verteilt Profiländerungen live; Clients patchen Mitglieder-/Freundes-/DM-Listen in place (`applyUserUpdate` in friends/dms-Store, `USER_UPDATE`-Zweig im servers-Store, Auth-Store für andere eigene Tabs). Anzeige: Status-Zeile im Mitglieder-Panel, in der DM-Liste und im Freunde-Panel (eigener Status-Text vor Online/Offline)
- **Hochladbare Profilbilder:** `POST /users/me/avatar` (roher Bild-Body ≤ 2 MiB, Magic-Byte-Prüfung PNG/JPEG/WebP/GIF, Rate-Limit 10/min) → MinIO `avatars/{userId}` mit Content-Type-Metadaten; `GET /users/:id/avatar` **öffentlich** (eigener Controller ohne AuthGuard – `<img>` kann keine Bearer-Header senden; UUID nicht erratbar, Trade-off in ROADMAP) mit `?v=`-Cache-Busting + immutable-Cache-Header. Client skaliert vor dem Upload auf 256-px-Quadrat (Mitte-Crop, `lib/profileImage.ts`). Neue wiederverwendbare `Avatar`-Komponente; Bild erscheint in Profilseite (Upload-Button mit Hover-Overlay), UserFooter, Mitglieder-Panel, Chat (`MessageRow` über senderAvatars-Map), DM-Liste, Freunde-Panel und Voice-Roster
- **Server-Profilbilder:** analog `POST /servers/:id/icon` (ManageServer) + öffentlicher `GET /servers/:id/icon`; `SERVER_UPDATE`-Event verteilt die neue iconUrl live. Anzeige im Server-Kopf der Sidebar (mit ManageServer als Upload-Button) und im ServerDock
- **Sicherheits-Härtung dabei:** `PATCH /users/me` (`avatarUrl`) und `PATCH /servers/:id` (`iconUrl`) akzeptieren keine freien URLs mehr (nur noch `''` = Bild entfernen) – Avatare/Icons werden bei ALLEN sichtbaren Nutzern als `<img>` gerendert, eine externe URL hätte deren IP-Adressen an Dritte geleakt (vorher harmlos, weil nur selbst sichtbar)
- **ServerDock entschlackt:** Bogen-SVG („Schale") und schwarzes Panel (`bg-zinc-950/95` + Rahmen) entfernt – nur noch frei schwebende Icons (Wunsch); leere Slot-Platzhalter raus, Hover hebt Icons an (`-translate-y`/`scale`), Scroll-Leiste erscheint nur noch bei >5 Servern, Server-Icons werden als Bild gerendert
- **Favicon:** Arians Logo (Downloads) auf 64/180 px skaliert → `apps/web/public/favicon.png` + `apple-touch-icon.png`, in `index.html` verlinkt
- **Animationen (dezent, nur transform/opacity, `prefers-reduced-motion` respektiert):** Nachrichten gleiten ein (`animate-msg-in`), Ansichten blenden weich (`animate-view-in` auf ChatView/FriendsPanel), Modals ploppen (`animate-pop-in`), Online-Punkt pulsiert (`animate-presence`), Login-Seite mit animiertem Aurora-Verlauf (`auth-aurora`), Dock-Icons mit Hover-Lift

**Verifiziert (erster Durchlauf, Browser gegen echte API + Docker-Infra, Wegwerf-Nutzer `owner_dlpmxt`/`member_dlpmxt`):** REST-Skript: Upload als Mitglied ohne Rolle 201 (100 KiB + 3 MiB), DM-Upload beidseitig 201, Nachricht mit attachmentIds 201 → Backend war fehlerfrei. UI als Mitglied OHNE Rolle: Bild in #allgemein angehängt (Chip erscheint – vor dem Fix nachweislich nicht) und gesendet, Bild gerendert; dasselbe in der DM. Voice: Beitritt → „Sprache verbunden“, erneuter Klick auf den Kanal → **bleibt verbunden** (vorher Rauswurf). Status: Owner setzt Status per REST → erscheint live im Mitglieder-Panel des Mitglieds. Avatar: Upload über die Profilseite → Bild kommt in UserFooter, Chat, Mitgliederliste und Voice-Roster an. Server-Icon: Owner-Upload per REST → 201, öffentlicher GET 200 `image/png`, Icon erscheint live (SERVER_UPDATE) im Server-Kopf UND im Dock beim Mitglied. Dock: kein SVG-Bogen, transparenter Hintergrund. Favicon 200. avatarUrl-Härtung: externe URL → 400, `''` → null. **Zweiter Durchlauf (nach Pull, auf Soundboard-Stand):** identische Änderungen erneut angewendet; Browser-Stichprobe (Bild-Anhang ohne Rolle, Voice-Doppelklick, Avatar-Upload, Dock/Favicon) erneut grün. Build (4 Workspaces)/Tests/Lint/Format grün.

### Verbesserungs-Runde (12.07.2026): Hyperlinks, Mikrofon-Empfindlichkeit, Soundboard-Lautstärke

Drei Verbesserungswünsche von Arian, alle rein clientseitig (kein API-/Schema-/Protokoll-Change):

- **Links werden Hyperlinks:** Neuer Parser `lib/links.ts` (`splitLinks`) erkennt `http://`/`https://`-URLs im entschlüsselten Nachrichtentext – bewusst NUR explizite Schemata (kein „www."-Raten, `javascript:` & Co. damit strukturell ausgeschlossen); Satzzeichen am Ende und unbalancierte Schluss-Klammern zählen nicht zur URL (Wikipedia-Links mit `(Klammern)` bleiben ganz). Rendering in `MessageRow` (`LinkifiedText` innerhalb der Nicht-Erwähnungs-Segmente) als `<a target="_blank" rel="noopener noreferrer">`
- **Mikrofon-Empfindlichkeit in den Einstellungen:** Neuer persistenter Client-Settings-Store `store/settings.ts` (zustand `persist` → localStorage `parley-settings`; bewusst Geräte- statt Account-Einstellung). Neue Sektion „Sprache & Audio“ auf der Profilseite (`components/AudioSettings.tsx`): Schwellen-Slider −100…0 dBFS (−100 = „Aus (immer übertragen)“ = bisheriges Verhalten, Standard) + **Live-Mikrofontest** mit Pegelbalken und Schwellen-Markierung (gleiche RMS→dBFS-Rechnung wie das Gate). Durchgesetzt wird die Schwelle als **WebAudio-Noise-Gate im `VoiceClient`** (`createMicGate`: Quelle → GainNode → MediaStreamDestination, produziert wird der bearbeitete Track; Analyser-Poll alle 50 ms, 400 ms Haltezeit gegen abgeschnittene Wortenden, weiche Gain-Rampen gegen Knackser). Die Schwelle wird bei jedem Poll frisch aus dem Store gelesen → **wirkt live während einer laufenden Verbindung**. Sprech-Erkennung („grüner Ring“) hängt jetzt am GESENDETEN Track – man leuchtet nur, wenn andere einen hören. Fallbacks: ohne WebAudio oder bei suspendiertem AudioContext wird das rohe Signal gesendet (sonst wäre das Mikro dauerhaft stumm)
- **Soundboard-Lautstärke live änderbar:** Lokaler Lautstärke-Multiplikator (`soundboardVolume` im Settings-Store) × Sound-Lautstärke; `lib/soundboard.ts` hält alle gerade spielenden Audio-Elemente in einem Set und ein Store-Subscribe passt deren `volume` **sofort** an – auch mitten in der Wiedergabe. Regler an zwei Stellen (gleicher Store): im `SoundboardDialog` und in „Sprache & Audio“

**Verifiziert (Browser gegen echte API + Voice + portable Infra, als `member_dlpmxt`/`owner_dlpmxt`):** Nachricht mit Wikipedia-Link `…Signal_(Messenger)`, `(siehe https://example.com).` und `www.ohne-schema.de` gesendet → genau zwei `<a>` mit korrektem href/target/rel, Klammer-Link vollständig, `).`-Satzzeichen abgetrennt, www-ohne-Schema bewusst NICHT verlinkt. Einstellungen: Slider auf −50 dB / 30 % → localStorage `parley-settings` korrekt, überlebt Logout/Login; Mikrofontest zeigt im Browser-Pane (Mikro blockiert) sauber den Fehlerpfad. Soundboard-Live-Test end-to-end: Sprachkanal „soundtest“ erstellt+beigetreten (Zuhörer-Modus), 8-s-WAV „voltest“ über die UI hochgeladen, Play → Wiedergabe startet mit `volume 0.3` (Sound 100 % × lokal 30 %); Regler WÄHREND der Wiedergabe auf 80 %/10 % → `el.volume` springt sofort auf 0.8/0.1 (Element spielt nachweislich weiter). Konsole über die gesamte Session fehlerfrei; Build (shared+web)/Lint/Format grün. **Offen für Arian:** Noise-Gate und Pegelanzeige einmal mit echtem Mikrofon gegentesten (im Browser-Pane ist Gerätezugriff blockiert)

### Quality-of-Life-Runde (13.07.2026): Multi-Browser-E2EE, DM-Sortierung, Profilkarte + Banner, Soundboard-Limits, Drag & Drop

Fünf Wünsche von Rijon, alle umgesetzt und verifiziert:

- **Eigene Nachrichten in jedem Browser lesbar (Multi-Browser-E2EE):** Bisher lebte die E2EE-Identität nur in der IndexedDB EINES Browsers – ein zweiter Browser desselben Accounts konnte v. a. die selbst geschriebenen Nachrichten nie entschlüsseln. Jetzt teilen sich alle Browser eines Accounts EINE Identität über ein **verschlüsseltes Schlüssel-Backup** auf dem Server (`KeyBackup`-Modell, `GET/PUT /api/keys/backup`): Der Client leitet beim Passwort-Login einmalig einen Backup-Schlüssel ab (PBKDF2-SHA-256, 600k Iterationen, WebCrypto – `crypto_pwhash` fehlt im Standard-libsodium) und lädt Identität+Prekey als secretbox-Blob hoch bzw. übernimmt die Identität eines vorhandenen Backups (`syncKeyBackup` in `lib/e2ee.ts`; `onlyIfMissing`-Flag löst das Erst-Upload-Rennen zweier Browser per 409 auf). Dazu zwei Protokoll-Anpassungen: **Sender-Keys werden auch an sich selbst verteilt** (Umschlag an die eigene – geteilte – Identität) und **Umschläge bleiben 30 Tage in der Mailbox liegen** (`ENVELOPE_RETENTION_DAYS`; Ack ist jetzt ein No-op, gelöscht wird per Retention in `listEnvelopes`, Abruf-Limit = Mailbox-Deckel 1000) – jeder Browser merkt sich selbst, was er verarbeitet hat (`processedEnvelopes`-Log auf 2000 erhöht; Fehlversuche werden pro Identität gemerkt und nach einer Backup-Übernahme erneut versucht). Der Envelope-Wipe bei Identitätswechsel in `registerKeys` ist entfernt (Umschläge an die Backup-Identität bleiben für andere Browser lesbar). Passwort wird nur im Speicher gestasht (`e2ee.stashLoginPassword` aus der AuthPage) und nach dem Abgleich verworfen. **[Hinweis 17.07.: Dieses Backup-Design wurde durch das Argon2id-Design der Runde „Backup + Anrufe“ (weiter unten) ersetzt – Self-Envelopes/Retention/`stashLoginPassword` sind nicht mehr im Code; die übrigen vier Punkte dieser Runde gelten unverändert.]**
- **DMs nach Aktivität sortiert:** `DmChannelInfo.lastMessageAt` (Server: jüngste Nachricht pro DM-Kanal in `listMine`, sortiert absteigend), Client sortiert nach `lastMessageAt ?? createdAt` und rückt DMs bei `MESSAGE_CREATE` live nach oben (`bumpActivity` im dms-Store, aufgerufen aus `handleMessageCreate`)
- **Profilkarte per Klick + Profil-Banner:** Klick auf Avatar/Namen (Chat-Nachrichten, Mitglieder-Panel, Freunde-Panel) öffnet eine Discord-artige Profilkarte (`UserProfileCard`, global über `useUiStore.openProfile`): Banner (Bild oder Farbverlauf), Avatar mit Online-Punkt, Name/Nickname, Status-Text, „Nachricht senden“ (öffnet den DM-Kanal und wechselt zur Home-Ansicht). Neues Feld `User.bannerUrl` (Migration `banner_and_key_backup`) überall in den Typen (`PublicUser`/`AuthUser`/`ServerMember`/`USER_UPDATE`); Upload wie beim Avatar (`POST /users/me/banner`, öffentlicher `GET /users/:id/banner`, Client-Crop auf 680×240), Verwaltung auf der Profilseite (ändern/entfernen, `bannerUrl: ''` löscht)
- **Soundboard-Limits raus:** Dauer-Limits komplett entfernt (`MAX_SOUNDBOARD_UPLOAD_SECONDS`/`MAX_SOUNDBOARD_PLAY_SECONDS` samt Wiedergabe-Stopper und Upload-Dauerprüfung; die Dekodierbarkeits-Prüfung vor dem Upload bleibt), Dateigröße von 1 auf **10 MiB** angehoben (gleiche Grenze wie Anhänge – mehr ließe der Raw-Body-Parser nicht durch)
- **Drag & Drop:** Dateien lassen sich in den Chat ziehen (Overlay „Dateien hier ablegen“, zählerbasiertes dragenter/dragleave, hängt sie wie die 📎-Auswahl an) und Audiodateien ins Soundboard-Formular (neue Drop-Zone ersetzt das nackte File-Input; Klick öffnet weiterhin die Dateiauswahl, Namensvorschlag aus dem Dateinamen bleibt)

**Verifiziert (Browser gegen echte API + portable Infra, Nutzer `fable_test`):** Multi-Browser-Kernszenario end-to-end: registrieren → Nachricht in #allgemein senden (Self-Envelope `POST /api/envelopes` 204) → **IndexedDB komplett gelöscht** (simuliert frischen Browser) → Passwort-Login → Backup geladen (GET 200), Identität übernommen, Umschläge aus der Mailbox verarbeitet → **eigene Nachricht ist im Klartext lesbar** (vorher: 🔒-Platzhalter). REST-Skript (11 Checks, alle grün): Backup-Endpunkt, DM-Sortierung inkl. Umsortierung nach neuer Nachricht + `lastMessageAt`, Soundboard 2 MiB → 201 und 11 MiB → 413, Banner-Upload/öffentlicher Abruf/Entfernen, `bannerUrl` in PublicUser. UI: Profilkarte öffnet aus Mitglieder-Panel (eigenes Profil ohne „Nachricht senden“-Knopf), DM-Liste zeigt neueste Unterhaltung oben und rückt bei Live-Nachricht (Gateway-Event) ohne Reload um, Drop-Overlay erscheint bei dragenter und die gedroppte Datei landet in der Anhang-Liste, DM-Senden nach dem Umbau weiter intakt. Migration `banner_and_key_backup` eingespielt; Build (shared)/Typecheck (api+web)/Tests (32)/Lint/Format grün.

### Verbesserungs-Runde (13.07.2026): Link-Vorschauen (Embeds)

Wunsch von Rijon: „embedded links" – Vorschau-Karten (Titel/Beschreibung/Bild) für Links im Chat, wie bei Discord. Klickbare Links gab es schon (`lib/links.ts`, 12.07.2026); neu ist die Karte. Nach Rückfrage zwei bewusste Entscheidungen: **externe Bild-URL referenzieren** (statt das Bild mitzuverschlüsseln) und **automatisch beim Senden** (statt Klick-zum-Laden).

- **E2EE-konsistent:** Die Vorschau-Metadaten reisen – wie Anhänge – IM verschlüsselten Nachrichtentext (`MessageContentV1.embeds`, neuer Typ `LinkEmbed` in `@parley/shared`). Der Server sieht die Karte NICHT; er sieht die URL nur einmal transient beim Unfurl. Beim Dekodieren werden die (absenderkontrollierten) Felder defensiv geprüft: `url`/`imageUrl` müssen http(s) sein (kein `javascript:`/`data:`), Texte werden geklemmt, Anzahl auf 4 gedeckelt
- **Server-Unfurl mit SSRF-Schutz** (`apps/api/src/unfurl/`, `POST /api/unfurl`, auth + rate-limited 30/min): Der Browser darf fremdes HTML wegen CORS nicht selbst lesen → der Server holt die OpenGraph-/`<title>`-Metadaten. Zwingender SSRF-Schutz, weil der Server sonst zu beliebigen (auch internen) Zielen Requests stellt: nur http(s), nur Standard-Ports (80/443), **DNS-Auflösung + Sperre privater/reservierter IP-Bereiche** (v4 inkl. CGNAT/Link-Local/Cloud-Metadaten 169.254.169.254, v6 inkl. `::1`/ULA/Link-Local/IPv4-mapped), **jede Weiterleitung wird erneut geprüft** (manuelles Folgen, max. 3), 5-s-Timeout, 512-KiB-Body-Deckel, nur `text/html`
- **Absender holt beim Senden** (`lib/unfurl.ts` → `buildEmbeds`): distincte http(s)-URLs aus dem Text (max. 4), parallel unfurlen, Ergebnis in den Ciphertext. Best-effort mit 6-s-Gesamt-Deckel – ein langsamer Host blockiert den Versand nicht (dann eben ohne Karte). Bearbeiten erhält bestehende Embeds (kein Re-Unfurl, wie bei Anhängen/Zitat)
- **Karte im Chat** (`EmbedCard` in `MessageRow`): linker Akzentbalken, Seitenname/Titel/Beschreibung/Bild; die ganze Karte ist der Link (`target=_blank`, `rel=noopener noreferrer`). Bild mit `referrerPolicy="no-referrer"` und `onError`-Ausblenden
- **Bewusster Trade-off (ROADMAP):** Das Vorschaubild zeigt auf den fremden Host – beim Rendern lädt der LESER-Browser es direkt, dessen IP wird also an den Host geleakt (Discord proxyt das serverseitig). Von Rijon so gewählt (einfacher, kein extra verschlüsselter Blob). Residual beim SSRF-Schutz: DNS-Rebinding zwischen Prüfung und Verbindung – für den Projektumfang akzeptiert, in ROADMAP notiert

**Verifiziert:** Shared-Unit-Tests (24, davon neu: Embed-Encode/Decode-Roundtrip, unsichere URL verworfen + Textlängen geklemmt, Anzahl gedeckelt). API-Unit-Tests (20, davon 9 neu für `UnfurlService` gegen gemocktes `fetch`/DNS): OpenGraph-Titel/Beschreibung/Bild inkl. HTML-Entity-Dekodierung und relativer Bild-Auflösung, `<title>`-Fallback, **SSRF-Blocks** (IP-Literale 127/169.254/10/192.168/`[::1]` ohne fetch, öffentlicher Name→interne IP = DNS-Rebinding, exotischer Port, Redirect-Ziel intern), Nicht-HTML ignoriert, unsicheres `og:image` verworfen. Build (shared+api+web)/Lint/Format grün. **Nicht in dieser Session:** Voller Browser-Interop-Test – die Infrastruktur (Docker/portable DB, API/Web, Zwei-Nutzer-E2EE, Internet-Zugriff fürs echte Unfurl) war in dieser Session nicht verfügbar. Zum Nachholen: `npm run dev` starten, in zwei Browsern einloggen, eine Nachricht mit URL senden → Karte muss beim Empfänger erscheinen.

### Feature-Runde: E2EE-Schlüssel-Backup + private Anrufe (13.07.2026)

Auf Arians Wunsch, selbst gebaut: (1) Login mit demselben Account auf einem neuen Gerät/Browser kann jetzt ALLE vorherigen Nachrichten lesen; (2) private Sprach-/Videoanrufe mit Freunden in DMs.

**1. E2EE-Schlüssel-Backup (Prinzip wie WhatsApp/Signal-Backups):**

- **Krypto (`packages/shared/src/crypto/backup.ts`, 3 neue Unit-Tests):** Zufälliger 256-Bit-Master-Key verschlüsselt den kompletten Client-Krypto-Zustand (XChaCha20-Poly1305, AD bindet die User-ID); der Master-Key selbst ist mit einem aus dem LOGIN-Passwort abgeleiteten Schlüssel umhüllt (Argon2id via `crypto_pwhash`, INTERACTIVE-Parameter). Dafür `libsodium-wrappers` → **`libsodium-wrappers-sumo`** (Standard-Variante hat kein pwhash; Web-Initial-Bundle 736 → 878 KB, gzip 242 → 292 KB)
- **API:** Prisma-Modell `KeyBackup` (Migration `key_backup`; nur Salt, KDF-Parameter, zwei Ciphertexte – Server kann nichts entschlüsseln), `GET/PUT /api/keys/backup` + `PUT /api/keys/backup/blob` (laufender Sync), Validierung (Blob ≤ ~1 MiB) + Rate-Limits; JSON-Body-Limit der API von 100 kB auf 2 MB (Body-Parser jetzt explizit registriert). **Beim Identitäts-Reset bleibt das Backup bewusst bestehen** – es enthält die alte Identität samt History-Schlüsseln, der nächste Passwort-Login stellt sie wieder her
- **Web-Client (`lib/e2ee.ts`):** `init` versucht VOR der Schlüsselerzeugung den Restore: Backup abrufen → Master-Key aus IndexedDB oder per Login-Passwort entpacken (`lib/loginSecret.ts` reicht es aus Login/Registrierung herüber, wird nach Erfolg gelöscht) → Blob importieren. Merge-Regeln: fremde Identität im Blob gewinnt (es IST die Konto-Identität, lokale 1:1-Sessions werden verworfen); `recvSenderKeys` immer auf den FRÜHESTEN Kettenstand (maximal viel History lesbar); Sessions/eigene Sender-Keys nach Zeitstempel. Ohne Backup-Datensatz richtet der erste Passwort-Login das Backup ein. Danach debounced Sync (3 s) nach jeder Zustandsänderung (Umschlag verarbeitet, Sender-Key erzeugt/ratcht, Rotations-Flag). Wichtig: Auf einem frischen Browser gibt es kein Refresh-Cookie → der Nutzer MUSS sich per Passwort anmelden, der Restore-Pfad ist also immer versorgt
- **Trade-offs in ROADMAP.md:** Offline-Brute-Force gegen das Passwort bei DB-Leak (= dieselbe Fläche wie der Login-Hash), Last-Write-Wins bei GLEICHZEITIG aktiven Geräten, lokaler IndexedDB-Passphrase-Schutz weiter offen

**2. Private Anrufe mit Freunden (DM-Kanäle über die bestehende SFU):**

- **API (`voice.service.ts`):** `join` akzeptiert jetzt DM-Kanäle (nur die zwei DmMember, 404 für Außenstehende, Blockierung → 403 wie beim Senden; Auszeit gilt nur für Server). Broadcast kanal-bewusst: Server-Kanäle → Mitglieder mit ViewChannels, DMs → die zwei Teilnehmer; `VoiceStateUpdatePayload` trägt neu `serverId` (null = DM). Beim frischen Anruf-Start (Gegenseite noch nicht drin, kein Reconnect-Rejoin) geht **`CALL_RING`** nur an den Angerufenen; `POST /voice/channels/:id/decline` schickt **`CALL_DECLINE`** nur an den Anrufer. Der SFU (apps/voice) brauchte KEINE Änderung – ein Raum pro Kanal-ID funktioniert für beide Kanalarten, Roster-Reconcile (Phase 14) räumt auch DM-Sessions
- **`DmChannelInfo.voiceStates`:** DM-Liste liefert den Anruf-Roster-Snapshot (wie `ServerDetails.voiceStates`); der DMs-Store spiegelt ihn in den Voice-Store
- **Web:** Voice-Store mit `dmCalls` (Kanal → Roster), `incomingCall`, `handleCallRing/Decline`, `declineCall`; `joinVoice(channel, { withCamera })` startet die Kamera nach dem Verbinden (🎥-Button). **`CallOverlay`** (in App, ansichtsunabhängig): Avatar + „ruft dich an“ + Annehmen/Ablehnen, WebAudio-Klingelton (`startRingtone` in `lib/sounds.ts`, kein Asset; verstummt bei Annehmen/Ablehnen/Auflegen der Gegenseite oder nach 45 s), Annehmen öffnet die DM und tritt bei. DM-ChatView: 📞/🎥 im Header, grünes „Anruf aktiv – Beitreten“-Banner, „Du bist im Anruf“; DM-Liste mit 📞-Indikator; `VoiceStage` (Video/Screen-Share) jetzt auch in der Home-Ansicht; VoicePanel zeigt „Im Anruf · 📞 @partner“ und blendet das Soundboard (Server-Feature) in DM-Anrufen aus. Ablehnen beim Anrufer: Hinweis-Dialog + auflegen

**Verifiziert:** Skript mit **29 Checks, alle grün** (startet die gebaute API selbst, echte DB/Redis; räumt Testdaten via Prisma ab): Backup-Roundtrip übers echte REST (Setup → Login „neues Gerät“ → Master-Key mit Passwort entpackbar, falsches Passwort scheitert, keine Klartext-Schlüssel im Datensatz → ALLE 3 Nachrichten – eigene UND fremde – mit den wiederhergestellten Schlüsseln lesbar); Anrufe (Ring mit Anrufer-Info nur an den Angerufenen, `serverId:null`, `voiceStates` in GET /dms, Decline erreicht nur den Anrufer, Reconnect-Rejoin klingelt NICHT erneut, Annehmen ohne Ring an den Wartenden, Mute-Broadcast im DM-Anruf, Außenstehende 404 auf join UND decline, Verlassen räumt mit state:null, Blockierung → 403). **Browser-Test** (Preview, api+voice+web): Registrierung → `PUT /keys/backup` automatisch; Nachricht senden → debounced `PUT /keys/backup/blob`; **IndexedDB komplett gelöscht + Logout (= fabrikneues Gerät) → Passwort-Login → `GET /keys/backup` 200, DIESELBE Identität erneut publiziert (kein Reset) und die alte Nachricht ist lesbar statt 🔒**. Anrufe im Browser: Skript-frieda ruft an → Overlay „bkui_frieda ruft dich an“ erscheint (auch in der Server-Ansicht), Ablehnen → frieda empfängt CALL_DECLINE; erneuter Anruf → Annehmen → Browser tritt bei (echter SFU, Zuhörer-Modus ohne Mikro), DM zeigt „Du bist im Anruf“, Panel „Im Anruf · 📞 @bkui_frieda“ ohne Soundboard, DM-Liste mit 📞; Auflegen → state:null bei frieda; Konsole über die gesamte Session fehlerfrei. Build (4 Workspaces)/Tests (11 API + 24 shared)/Lint/Format grün

### Verbesserungs-Runde (16.07.2026): Mikrofon-Auswahl (Eingabegerät)

Wunsch von Rijon: Man soll auswählen können, welches Mikrofon benutzt wird. Rein clientseitig (kein API-/Protokoll-Change).

- **Settings-Store:** `micDeviceId` (null = Systemstandard) im persistenten `parley-settings`-Store (bewusst Geräte- statt Account-Einstellung, wie die übrigen Audio-Werte). Neuer Helfer `getMicStream()` öffnet das Mikrofon: erst mit `deviceId: { exact } ` fürs gewählte Gerät, bei Fehlschlag (abgezogen/belegt) Fallback aufs Standardgerät. **Wichtig – erste Fassung mit `ideal` wechselte in der Praxis NICHT** (von Rijon beobachtet): Ein weiches `ideal` erlaubt dem Browser, bei einer bereits offenen Standard-Quelle zu bleiben; nur `exact` erzwingt das Gerät. Der Helfer liegt im Settings-Store statt in `lib/voice.ts`, damit `AudioSettings` ihn nutzen kann, ohne mediasoup-client in den Haupt-Bundle zu ziehen (Code-Splitting bleibt intakt)
- **VoiceClient (`lib/voice.ts`):** `startMic` nutzt `getMicStream()`; neue Methode `restartMic(paused)` schaltet das Gerät **live während einer Verbindung** um: neuer Track per `producer.replaceTrack` (kein Rejoin, kein neues Signaling, Pausenzustand bleibt), Noise-Gate und Sprech-Erkennung ziehen auf den neuen Track um; das neue Gerät wird ZUERST beschafft und der alte Pfad erst nach erfolgreichem Tausch abgebaut (kein Aussetzer, bei Fehlschlag läuft das alte Gerät weiter); im Zuhörer-Modus (bisher kein Mikro) wird ein kompletter Neustart versucht – mit dem neuen Gerät klappt es evtl. jetzt
- **Voice-Store:** `applyMicDevice()` reicht den Wechsel an den laufenden Client durch und aktualisiert `hasMic`
- **UI (`AudioSettings`):** „Eingabegerät“-Dropdown in „Sprache & Audio“ (Standard + alle `audioinput`-Geräte, `devicechange`-Listener aktualisiert bei An-/Abstecken, Platzhalter-Option „Gespeichertes Gerät (nicht gefunden)“ falls das gespeicherte Gerät fehlt, „Jetzt freigeben“-Button, weil Browser Gerätenamen erst nach einer Mikrofon-Freigabe hergeben); der Mikrofontest nutzt das gewählte Gerät und startet bei Gerätewechsel neu

**Verifiziert (Browser-Pane, Gerätezugriff dort blockiert → Logik-Nachweis per Stub):** UI rendert (Dropdown, Freigabe-Hinweis-Pfad); Auswahl persistiert in `parley-settings` und übersteht Reload (Platzhalter-Option greift, wenn das Gerät nicht in der Liste ist); gUM-Stub protokolliert die Constraints des Mikrofontests: **1. Aufruf `deviceId:{exact:"…"}`, nach Fehlschlag 2. Aufruf ohne deviceId (Standard-Fallback)** – exakt die gewollte Reihenfolge; Zurücksetzen auf „Standard“ speichert `null`; Konsole fehlerfrei; Build (tsc+vite)/Lint/Format grün. **Offen für Rijon:** Wechsel zwischen zwei echten Mikrofonen gegentesten – einmal vor dem Beitritt und einmal WÄHREND einer laufenden Sprachverbindung (im Pane ist Gerätezugriff blockiert)

## Nächste Schritte

Der Phasenplan ist abgeschlossen. Alles Weitere steht priorisiert in [ROADMAP.md](ROADMAP.md) („Stand“-Abschnitt oben): zuerst die Deployment-Härtung (Secrets, TLS, Reverse-Proxy, VAPID, SPA-Fallback), wenn die App echte Nutzer bekommen soll. Die technische Grundlage dafür (CI/CD auf den VPS) steht – siehe [DEPLOY.md](DEPLOY.md); offen sind die einmaligen Schritte auf dem VPS selbst (Bestandsaufnahme, .env, Reverse Proxy, DNS, GitHub-Secrets).

## Dev-Umgebung (Stand Session 3, 05.07.2026)

Diese Maschine war frisch aufgesetzt (kein Node, kein Docker, WSL defekt). Docker Desktop braucht Admin + WSL → stattdessen läuft die Infrastruktur **portabel ohne Admin-Rechte**:

- **Node 22.23.1** portabel: `%LOCALAPPDATA%\Programs\nodejs` (im Benutzer-PATH)
- **PostgreSQL 16.6** (EnterpriseDB-Binaries): `%LOCALAPPDATA%\Programs\parley-infra\pgsql`, Datenverzeichnis `%LOCALAPPDATA%\parley-data\pgdata`, User/PW/DB wie in `.env`
- **Redis 5.0.14** (tporadowski-Windows-Port): `%LOCALAPPDATA%\Programs\parley-infra\redis`
- **MinIO**: `%LOCALAPPDATA%\Programs\parley-infra\minio.exe`
- **Alles starten (seit 11.07.2026): `npm run dev`** – ein Befehl für alles: shared bauen → Infrastruktur sicherstellen (`scripts/ensure-infra.mjs`: läuft schon alles, passiert nichts; sonst Docker Compose mit `--wait`, nur die fehlenden Services; ohne Docker-Daemon portable Infra via `dev-infra.ps1`) → `prisma migrate deploy` → API + Voice + Web parallel über `concurrently` (Labels api/voice/web, Strg+C beendet alle). Doppelklickbar: `scripts\dev.cmd`. Nur Infra: `npm run dev:infra` bzw. weiterhin `powershell -ExecutionPolicy Bypass -File scripts\dev-infra.ps1`
- `.env` wurde neu erzeugt (frisches `JWT_SECRET`), Migrationen mit `prisma migrate deploy` eingespielt

## Notizen für kommende Sessions

- 17.07.2026 (YouTube-/Spotify-Player-Vorschauen): Discord-artige, abspielbare Vorschauen für YouTube- und Spotify-Links. **Client:** neues `apps/web/src/lib/mediaEmbeds.ts` (deterministische Erkennung von youtube.com/youtu.be/shorts/live/music + open.spotify.com track/album/playlist/episode/show/artist, inkl. `?t=`-Startzeit) + `EmbedList`/`YoutubeCard`/`SpotifyCard` in `MessageRow.tsx`: YouTube = Thumbnail (i.ytimg.com, deterministisch aus der Video-ID) mit Play-Button, Klick lädt `youtube-nocookie.com`-iframe; Spotify = offizieller Embed-Player direkt (lazy). Erkennung läuft beim LESER aus den URLs (Text + Embeds) – Player erscheinen also auch ohne erfolgreichen Unfurl und rückwirkend für alte Nachrichten; iframe-URLs werden nie vom Absender übernommen, sondern aus der geprüften ID gebaut. **Server:** `UnfurlService` fragt für YouTube/Spotify jetzt zuerst die offiziellen oEmbed-JSON-Endpunkte ab (fest verdrahtete Hosts, Nutzer-URL nur als kodierter Query-Parameter) – Grund: YouTubes `og:title` liegt bei ~Byte 645.000, also HINTER dem 512-KiB-Lese-Limit des HTML-Scrapers, weshalb YouTube-Unfurls vorher `null` lieferten. 4 neue Vitest-Fälle in `unfurl.service.spec.ts` (13/13 grün). Verifiziert Ende-zu-Ende im Browser-Pane (Karte mit oEmbed-Titel „Rick Astley …", Play-Klick → nocookie-iframe mit `start=43` aus `?t=43`, Spotify-iframe 152 px, Reload/History ok). Browser-Pane-Eigenheiten wie in Session 19 (Enter submittet nicht → `form.requestSubmit()`; Screenshots timeouten; `loading="lazy"`-Bilder laden in der Pane nicht, weil sie nicht malt – im echten Browser unkritisch). Wegwerf-Nutzer `embedtester` (embedtester@test.local, Passwort `embed-test-1234`) mit Server „Embed-Test" in der Docker-DB **bewusst stehengelassen** zum Ausprobieren
- 17.07.2026 (Feature-Restaurierung nach dem Hotfix): Rijons vier Commits (`b566aeb` Quality-of-Life/Mobile/Typing/@-Picker, `0571dc9` Banner/Profilkarte + altes Backup, `9a77e87` Attachment-View, `0bfbad3` Embedded Links) per 3-Wege-Patch (`git diff c5cc404 0bfbad3` + `git apply --3way`) auf main zurückgeholt. **Bewusst NICHT übernommen** (etoe-Design hat gewonnen): Rijons Schlüssel-Backup-Format samt `stashLoginPassword`/`ENVELOPE_RETENTION_DAYS`/`save-key-backup.dto.ts`, deren `lib/e2ee.ts`- und `keys`-Modul-Änderungen. Hand-gemergt: `dms.service` (voiceStates + lastMessageAt), `shared/friends.ts`, Voice-Store (Anrufe + Join/Leave-Sounds, `channelPeers` wird auch aus `dmCalls` geseedet), ChatView/HomeView/MainPage/ChannelSidebar (Mobile-Drawer + Anruf-UI + VoiceChannelView koexistieren); `lib/sounds.ts` = Rijons WebAudio-Chimes + etoe-Klingelton, die `.ogg`-Assets sind raus. **Kein lokaler Typecheck möglich (kein Node/Docker auf der Maschine)** – der Docker-Build in der CI ist das Gate; schlägt er fehl, bleibt die alte Version einfach live
- 17.07.2026 (Hotfix Produktions-502): Der Merge `aa992e9` vom 16.07. hat auf API-/Shared-/Web-Seite **komplett die etoe-Seite gewinnen lassen** und damit Rijons Arbeit verworfen (Unfurl/„embedded links“, Attachment-View, Banner, altes Schlüssel-Backup-Format) – inkl. Ersetzung der bereits auf Produktion angewandten Migration `20260713004444_banner_and_key_backup` durch `20260713182842_key_backup`. Folge: `CREATE TABLE "KeyBackup"` schlug auf dem VPS fehl (Tabelle existierte schon), der API-Container crash-loopt seitdem → 502 beim Login. Fix: alte Migration wiederhergestellt, neue Migration auf `DROP TABLE IF EXISTS` + Neuanlage umgeschrieben (alte Backup-Blobs sind mit dem neuen Format ohnehin unlesbar → gehen verloren, Backups entstehen beim nächsten Passwort-Login neu), `User.bannerUrl` ins Schema übernommen, **einmaliger** `prisma migrate resolve --rolled-back`-Schritt im Deploy-Workflow (nach erfolgreichem Deploy wieder entfernen!). Rijons verworfene Features: siehe Folgeeintrag
- 16.07.2026 (Mikrofon-Auswahl): Infrastruktur lief **portabel** (Postgres war schon oben, Redis/MinIO einzeln per `Start-Process` wie in `dev-infra.ps1` gestartet – `npm run dev:infra` hing, vermutlich beim Docker-Versuch). API/Web über die Preview-Configs; verifiziert als `fable_test` (Passwort `testpasswort123`). Keine Migration, keine neuen Wegwerf-Nutzer. Merken: Mikrofon-`deviceId`-Constraints im gUM immer mit `exact` (+ Fallback) statt `ideal` – `ideal` wechselt bei bereits offener Aufnahme nicht zuverlässig
- Session 20 (13.07.2026, Backup + Anrufe): **Docker-Infra aktiv**; Migration `key_backup` per `prisma migrate dev` eingespielt (13 Migrationen). Wegwerf-Nutzer in der Docker-DB **bewusst stehengelassen** zum Ausprobieren: `bkui_arian` / `bkui_frieda` (`…@test.local`, Passwort `test-passwort-123`, befreundet, DM-Kanal vorhanden, arian hat Server „Backup Test“). Die `bkcall_…`-Skript-Nutzer wurden abgeräumt. **Wichtig:** `packages/shared` hängt jetzt von `libsodium-wrappers-sumo` ab – nach dem Pull einmal `npm install`. Backup-Restore greift nur bei PASSWORT-Login (Session-Restore per Cookie hat das Passwort nicht); bestehende Nutzer bekommen ihr Backup automatisch beim nächsten echten Login
- Session 20 (13.07.2026, Quality-of-Life-Runde, Rijon): Infrastruktur lief **portabel** (Docker nicht im PATH; Postgres via `npm run dev:infra`, Redis/MinIO einzeln per `Start-Process` wie in `dev-infra.ps1` – der Skript-Aufruf mit `-ExecutionPolicy Bypass` wird vom Berechtigungs-Classifier blockiert). Migration `banner_and_key_backup` (13 Migrationen gesamt) per `prisma migrate dev`. **Wichtig für bestehende Nutzer:** Das Schlüssel-Backup entsteht erst beim nächsten **Passwort-Login** – wer den Multi-Browser-Support will, meldet sich in jedem Browser einmal ab und neu an (der Browser, der sich zuerst anmeldet, stellt die gemeinsame Identität; die anderen übernehmen sie beim Login). Alte Nachrichten von VOR dieser Runde bleiben nur in dem Browser lesbar, der sie geschrieben hat. Wegwerf-Nutzer in der portablen Dev-DB: `fable_test` (fable_test@example.com), `bob_mriitz83`/`carla_mriitz83` (…@example.com, Passwort jeweils `testpasswort123`) mit Server „Testserver“ (Sound „GrosserSound“, 2-MiB-Dummy – kein echtes Audio) und Fake-Ciphertext-DMs – **bewusst stehengelassen**. Browser-Pane-Screenshots liefen erneut in Timeouts; Verifikation über DOM-Reads/Netzwerk-Traces/REST-Skripte (`scratchpad/verify-features.mjs`)
- Session 19 (12.07.2026, Verbesserungs-Runde): **Docker-Infra aktiv** (Postgres/Redis/MinIO healthy, dieselbe Docker-DB wie Session 18). Im Server „Repro dlpmxt“ **bewusst stehengelassen** zum Ausprobieren: Sprachkanal „soundtest“ mit Soundboard-Sound „voltest“ (8-s-Testton) sowie eine „Link-Test“-Nachricht in #allgemein. Screenshots im Browser-Pane liefen erneut in Timeouts und Gerätezugriff (Mikrofon) ist dort blockiert – Verifikation lief über DOM-Reads/JS; Enter im Nachrichten-Eingabefeld löste im Pane die implizite Formular-Übermittlung nicht aus (`form.requestSubmit()` als Workaround; in echten Browsern funktioniert Enter)
- Session 18 (12.07.2026, Bugfix-Runde): **Docker-Infra aktiv** (Docker Desktop musste erst gestartet werden, dann `docker compose -f infra/docker-compose.yml up -d`; die Docker-DB brauchte danach `prisma migrate deploy` für die `soundboard`-Migration – Rijons Session 17 lief gegen die portable DB). Wegwerf-Nutzer `owner_dlpmxt`/`member_dlpmxt` (`…@test.local`, Passwort `Passwort123!`) mit Server „Repro dlpmxt“ in der Docker-DB. **Achtung:** Die Session musste komplett wiederholt werden, weil Arian nach dem ersten Durchlauf `git pull` ausführte und dabei den Working Tree (inkl. untracked Dateien) verwarf – vor Sessions mit lokalen Änderungen immer erst committen/stashen. **Wichtig fürs Deployment:** Die Fixes sind erst auf der Live-Site, wenn auf `main` gepusht wurde (GitHub-Actions-Deploy) – der gemeldete Bild-Versand-Bug steckt im Web-Client-Build
- Session 17 (12.07.2026, Soundboard): Infrastruktur lief **portabel** (Docker nicht aktiv); Postgres kam über `npm run dev:infra`, Redis/MinIO wurden nach den `dev-infra.ps1`-Schritten einzeln gestartet. Migration `soundboard` (12 Migrationen gesamt) per `prisma migrate dev` eingespielt (inkl. Backfill: Standardrollen |= 4096). `DEPLOY.md`/`ROADMAP.md` hatten vorbestehende Prettier-Abweichungen (aus den Commits „frank“/„Deploy“) – mit formatiert, damit `format:check` wieder grün ist. Wegwerf-Nutzer in der Dev-DB: `sbown_/sbmem_/sbspec_/sbout_…` (Skript, Testserver wieder gelöscht) und `sbui_own_mrhx3p1b`/`sbui_bot_mrhx3p1b` (UI-Test; Server „SB UI Test“ mit Sprachkanal „talk“ und Sound „Tada“ **bewusst stehengelassen** zum Ausprobieren; Passwort jeweils `test-passwort-123`). Screenshots im Browser-Pane liefen in Timeouts (Mikrofon-Hinweis-Overlay?) – Verifikation lief über Netzwerk-Traces/DOM-Reads
- Session 16 (11.07.2026, Phase 15 Schritt 5): Arbeitsumgebung war teilweise frisch: `node_modules` unvollständig (`npm install` + `npm run prisma:generate` nötig), **`.env` fehlte** (aus `.env.example` neu erzeugt, frisches `JWT_SECRET`), **`.claude/launch.json` fehlte** (neu angelegt, Configs api/voice/web auf `scripts/dev-*.cmd`). Infrastruktur läuft **portabel** (Docker nicht im PATH): Postgres via ensure-infra, Redis/MinIO manuell nach den `dev-infra.ps1`-Schritten. **Die portable DB war vom Migrationsstand abgedriftet** (fremde `Ban`-Tabelle mit `createdBy`, `Membership.timedOutUntil` statt `timeoutUntil` – Migration `moderation` als „failed“ markiert) → mit Arians Zustimmung per `prisma migrate reset` geleert und sauber auf alle 11 Migrationen gebracht; **alle früheren Testdaten sind weg** (auch `arian.test`/`frieda.test`). Neue Wegwerf-Nutzer in der DB: `p15opfer` (p15opfer@example.com) und `p15mod_…` (Passwort jeweils `test-passwort-123`); Testserver wurden wieder gelöscht

- Projektname „Parley“ ist nur Arbeitstitel (siehe ROADMAP.md)
- Node ist NICHT systemweit installiert – falls `node` im PATH fehlt: `%LOCALAPPDATA%\Programs\nodejs`
- Server starten: **`npm run dev`** (alles auf einmal, s. „Dev-Umgebung“) oder einzeln über die Preview-Panel-Konfigurationen `api`/`voice`/`web` (`.claude/launch.json`, nutzen `scripts/dev-*.cmd`) bzw. `npm run dev:api` / `npm run dev:voice` / `npm run dev:web`. Achtung: `npm run dev` und die Preview-Configs teilen sich die Ports 3001/3002/5173 – nicht doppelt starten
- Infrastruktur: **Docker Desktop war in Session 6 (07.07.2026) nicht mehr installiert** – es läuft wieder die portable Infra (`%LOCALAPPDATA%\Programs\parley-infra`, alle Binaries vorhanden): PostgreSQL per `pg_ctl -D %LOCALAPPDATA%\parley-data\pgdata -w start`, Redis/MinIO per `Start-Process` (Schritte wie in `scripts\dev-infra.ps1`; der Skript-Aufruf mit `-ExecutionPolicy Bypass` kann vom Berechtigungs-Classifier blockiert werden → Schritte einzeln ausführen). Die PORTABLE DB stand auf Phase-5-Stand; `e2ee`/`friends_dms`/`attachments` wurden per `prisma migrate deploy` nachgezogen. Die Docker-DB (Sessions 5) samt ihrer Testdaten ist damit nicht mehr im Zugriff. Falls Docker wieder da ist: `docker compose -f infra/docker-compose.yml up -d`
- Testnutzer in der (portablen) Dev-DB: `arian.test@example.com` / `frieda.test@example.com` (Benutzernamen `arian`/`frieda`), Passwort jeweils `test-passwort-123`
- Session 4 (06.07.2026): Dev-DB stand nur auf der Phase-1-Migration – `servers_channels`, `messages` und `roles` per `prisma migrate deploy` nachgezogen; Server „Arians Treffpunkt“ (Owner `arian_test`) neu angelegt, alte Server/Nachrichten waren weg
- `core.autocrlf=true` ist global gesetzt – `.gitattributes` pinnt deshalb LF für den Working Tree; nach einem frischen Checkout ggf. einmal `git add --renormalize .` bzw. Prettier laufen lassen
- Session 7 (08.07.2026, Phase 10): Die portable Infra (`parley-infra`) war wieder weg, **Docker Desktop dagegen zurück** – Infrastruktur läuft jetzt wieder per `docker compose -f infra/docker-compose.yml up -d` (Postgres/Redis/MinIO, healthy). Die Docker-DB stand bereits auf allen 7 Migrationen; `voice` per `prisma migrate dev` ergänzt. Docker-Daemon ggf. erst per „Docker Desktop.exe" starten (Daemon-Bereitschaft mit `docker info` abwarten). **Neuer dritter Dienst:** Voice-SFU – zum Testen von Sprachchat muss `npm run dev:voice` (Port 3002) zusätzlich zu API/Web laufen (Preview-Config `voice`). Phase 10 hat einen „Voice Testserver" (Owner `voicetester`) samt `peer2_…`-Wegwerfnutzern in der Docker-Dev-DB hinterlassen
- Session 8 (08.07.2026, Phase 11): Docker-Infra lief weiter (Postgres/Redis/MinIO auf 5432/6379/9000). Migration `voice_video` (VoiceSession.cameraOn/screenOn) per `prisma migrate dev` eingespielt. Prisma-Client ließ sich nur regenerieren, nachdem der laufende API-Dev-Watcher gestoppt war (das laufende `dist/main` hält die `query_engine-windows.dll.node` → `EPERM` beim `prisma generate`; API-Prozessbaum killen, dann generieren). Zum Verifizieren wurden api/web/voice über die Preview-Configs (neu) gestartet. Video-Konsum ist headless verifizierbar (Browser konsumiert einen vom Node-Skript fabrizierten VP8-Stream) – echte Kamera-/Screen-Aufnahme bleibt manueller Test. Der Sprachkanal „Herp" in „Arians Treffpunkt" diente als Testkanal
- Session 15, Nachtrag (11.07.2026): **Ein-Befehl-Start `npm run dev`** eingerichtet (auf Wunsch von Arian, vor den restlichen Phase-15-Punkten): `scripts/ensure-infra.mjs` + `concurrently` (Root-devDependency) + `scripts/dev.cmd`, README umgeschrieben. Verifiziert: Kompletter Kaltstart über `npm run dev` (Infra lief: „läuft bereits“-Pfad; Migrationen no-op; alle drei Dienste hoch, Health 200, Web lädt, Konsole sauber) UND Docker-Pfad isoliert (Redis-Container gestoppt → `ensure-infra` startet NUR Redis via Compose `--wait` neu, API/Voice verkraften den Aussetzer per ioredis-Reconnect). Die 2 npm-audit-Highs sind vorbestehend (multer via @nestjs/platform-express), nicht von concurrently
- Session 15 (11.07.2026, Phase 15 Schritt 4): **Docker-Infra aktiv** (Postgres/Redis/MinIO healthy, `com.docker.backend` lauscht auf 5432/6379/9000) – dieselbe Docker-DB wie in den Schritt-2/3-Sessions (Nutzer `ui15arian`/`ui15frieda_…`, `p15a/b_…` vorhanden; `arian.test`/`frieda.test` liegen nur in der portablen DB). Wegwerf-Nutzer `onboard_p15` (onboard.p15@example.com, Passwort `test-passwort-123`) mit offener Freundschaftsanfrage an `ui15arian` hinterlassen; die Testserver der Session wurden wieder gelöscht
- Session 11 (09.07.2026, Phase 14): Infra lief bereits portabel (Postgres/Redis/MinIO auf 5432/6379/9000), DB auf allen 11 Migrationen – **keine neue Migration** in dieser Phase (reiner Code-/Skalierungs-Umbau). `.env`-`JWT_SECRET` war ein echtes 64-Byte-Secret (kein Platzhalter), `TRUST_PROXY` leer (Dev). Verifiziert mit einem eigenständigen Node-Skript (`scratchpad/verify-phase14.cjs`), das SFU (`node apps/voice/dist/main.js`) und API (`node apps/api/dist/main.js`) selbst startet/killt (`taskkill /T /F`) und den echten `PresenceService` gegen Live-Redis lädt – Dienste mussten NICHT per Preview-Config laufen. Wichtiger Kniff: das Skript lädt `.env` selbst (dotenv) und vererbt `process.env` an die Kinder, damit API/SFU ihre Config sicher bekommen. Der **Boot-Reconcile** (`VoiceService.onModuleInit`) läuft VOR `app.listen` → beim API-Ready-Log ist der Abgleich schon durch. Keine Wegwerf-Nutzer in der DB hinterlassen (das Skript räumt `p14*`-Nutzer/Server + Redis-Keys im finally wieder ab)
- Session 10 (09.07.2026, Phase 13): Infra lief bereits (Postgres/Redis/MinIO auf 5432/6379/9000, DB auf allen 10 Migrationen); Migration `moderation` per `prisma migrate dev` ergänzt (11 Migrationen). **`.env` war frisch aus `.env.example`** – `JWT_SECRET` stand auf dem Platzhalter (API startete nicht: „JWT_SECRET fehlt oder ist der Beispielwert“) → neues 64-Hex-Secret erzeugt (`node -e "crypto.randomBytes(32).toString('hex')"`) und eingetragen; API **und** Voice-SFU danach neu gestartet (der SFU verifiziert Voice-Tokens mit demselben Secret – sonst schlägt Voice-Auth fehl). VAPID-Keys leer → Web-Push deaktiviert (für Phase 13 irrelevant). Dienste zum Verifizieren über die Preview-Configs `api`/`voice`/`web`. Wegwerf-Nutzer `p13*` (Skript) und `uiown…/uimem…` mit Server „UI P13 …“ (UI-Test) in der Dev-DB hinterlassen
- Session 9 (08.07.2026, Phase 12): **Docker wieder weg, portable Infra zurück** (`%LOCALAPPDATA%\Programs\parley-infra` vollständig) – Postgres/Redis/MinIO einzeln gestartet (Schritte wie `scripts\dev-infra.ps1`). Die portable DB stand auf Phase-8-Stand; `voice`/`voice_video`/`invites_and_push` per `prisma migrate deploy` nachgezogen (Server/Nachrichten der Docker-DB aus Session 7/8 sind hier nicht sichtbar). **VAPID-Schlüssel** für Web-Push erzeugt und in `.env` eingetragen (`node -e "console.log(require('web-push').generateVAPIDKeys())"`) – ohne sie ist Push deaktiviert. Push-Zustellung lokal testbar über einen HTTPS-Receiver mit selbstsigniertem Zertifikat + `NODE_EXTRA_CA_CERTS` beim API-Start (echte Browser-Push-Dienste brauchen HTTPS-Deployment, siehe ROADMAP). Wegwerf-Nutzer `p12*_…` (Skript) und `ui_owner_p12`/`ui_joiner_p12` mit Server „UI Phase12 Test" (UI-Test) in der portablen Dev-DB hinterlassen
