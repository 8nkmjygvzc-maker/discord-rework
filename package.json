{
  "name": "parley",
  "version": "0.1.0",
  "private": true,
  "description": "Parley (Arbeitstitel) – Ende-zu-Ende-verschlüsselte Chat-/Voice-/Video-Plattform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @parley/shared && npm run build -w @parley/api && npm run build -w @parley/web",
    "build:shared": "npm run build -w @parley/shared",
    "dev:api": "npm run dev -w @parley/api",
    "dev:web": "npm run dev -w @parley/web",
    "start:api": "npm run start -w @parley/api",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.4.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.62.0"
  }
}
